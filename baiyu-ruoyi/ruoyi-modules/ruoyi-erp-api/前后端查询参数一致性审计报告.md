# 前后端查询参数一致性审计报告

## 一、审计概述

**审计时间**: 2024年
**审计范围**: 前端 BusinessConfigurable.vue ↔ 后端 ErpEngineController.java
**审计目标**: 确保前端传递的所有查询条件后端都能正确解析

## 二、前端查询参数构建逻辑

### 2.1 参数来源
**文件**: `BusinessConfigurable.vue:711-770`

**构建流程**:
```javascript
const buildMainQueryConfig = () => {
  const conditions = []
  
  // 从 searchConfig 构建查询条件
  const searchFields = parsedConfig.search?.fields || []
  
  searchFields.forEach(field => {
    let value = queryParams.value[field.field]
    const operator = field.queryOperator || 'eq'  // 默认操作符
    
    // 日期范围特殊处理
    if (field.component === 'daterange') {
      if (Array.isArray(dateRange.value) && dateRange.value.length === 2) {
        value = dateRange.value  // 数组格式
      } else {
        return  // 日期范围为空，跳过
      }
    }
    
    // 跳过空值
    if (value === undefined || value === null || value === '') {
      return
    }
    
    // 构建条件
    if (field.component === 'daterange') {
      conditions.push({
        field: field.field,
        operator: operator,  // 通常为 'between'
        value: value  // 数组格式
      })
    } else if (Array.isArray(value)) {
      // IN 条件
      conditions.push({
        field: field.field,
        operator: operator,  // 通常为 'in'
        value: value
      })
    } else {
      // 单个值条件
      conditions.push({
        field: field.field,
        operator: operator,
        value: value
      })
    }
  })
  
  return {
    conditions: conditions,
    orderBy: orderBy
  }
}
```

### 2.2 前端支持的操作符
| 操作符 | 来源 | 说明 |
|--------|------|------|
| `eq` | 默认值 | 精确查询 |
| `between` | daterange 组件 | 范围查询 |
| `in` | 数组值 | IN 查询 |
| 其他 | `field.queryOperator` | 配置指定 |

### 2.3 前端参数格式
| 场景 | 参数格式 | 示例 |
|------|----------|------|
| 单值查询 | `{field, operator, value}` | `{"field": "name", "operator": "like", "value": "张"}` |
| 日期范围 | `{field, operator, value[]}` | `{"field": "FDate", "operator": "between", "value": ["2026-03-01", "2026-03-30"]}` |
| IN 查询 | `{field, operator, value[]}` | `{"field": "status", "operator": "in", "value": [1, 2, 3]}` |

## 三、后端查询参数解析逻辑

### 3.1 参数接收
**文件**: `ErpEngineController.java:87-152`

**接收流程**:
```java
@PostMapping("/query/execute")
public R<?> executeDynamicQuery(@RequestBody Map<String, Object> params) {
    String moduleCode = (String) params.get("moduleCode");
    String tableName = (String) params.get("tableName");
    Map<String, Object> queryConfig = (Map<String, Object>) params.get("queryConfig");
    
    // 构建查询条件
    QueryWrapper<Object> queryWrapper = buildQueryFromBuilderMode(queryWrapper, queryConfig);
    
    // 执行查询
    Page<Map<String, Object>> page = dataPermissionService.selectPageByModuleWithTableName(
        moduleCode, tableName, pageQuery, queryWrapper
    );
    
    return R.ok(result);
}
```

### 3.2 后端支持的操作符
**文件**: `ErpEngineController.java:1426-1473`

| 操作符 | 代码位置 | 说明 | 参数类型 |
|--------|----------|------|----------|
| `eq` | 第1427-1429行 | 精确查询 | Any |
| `ne` | 第1430-1432行 | 不等于 | Any |
| `gt` | 第1433-1435行 | 大于 | Number/Date |
| `ge` | 第1436-1438行 | 大于等于 | Number/Date |
| `lt` | 第1439-1441行 | 小于 | Number/Date |
| `le` | 第1442-1444行 | 小于等于 | Number/Date |
| `like` | 第1445-1447行 | 模糊匹配 | String |
| `left_like` | 第1448-1450行 | 左模糊 | String |
| `right_like` | 第1451-1453行 | 右模糊 | String |
| `in` | 第1454-1458行 | IN 条件 | List |
| `between` | 第1459-1464行 | BETWEEN 条件 | List |
| `isNull` | 第1465-1467行 | IS NULL | - |
| `isNotNull` | 第1468-1470行 | IS NOT NULL | - |

### 3.3 后端参数解析
**文件**: `ErpEngineController.java:1403-1500`

```java
private QueryWrapper<Object> buildQueryFromBuilderMode(
    QueryWrapper<Object> queryWrapper,
    Map<String, Object> queryConfig) {
    
    // 1. 解析 conditions 数组
    List<Map<String, Object>> conditions = (List<Map<String, Object>>) queryConfig.get("conditions");
    for (Map<String, Object> condition : conditions) {
        String field = (String) condition.get("field");
        String operator = (String) condition.get("operator");
        Object value = condition.get("value");
        
        switch (operator) {
            case "eq":
                queryWrapper.eq(field, value);
                break;
            case "between":
                if (value instanceof List && ((List<?>) value).size() >= 2) {
                    List<?> values = (List<?>) value;
                    queryWrapper.between(field, values.get(0), values.get(1));
                }
                break;
            // ... 其他操作符
        }
    }
    
    // 2. 解析排序配置
    List<Map<String, Object>> orderBy = (List<Map<String, Object>>) queryConfig.get("orderBy");
    for (Map<String, Object> order : orderBy) {
        String field = (String) order.get("field");
        String direction = (String) order.getOrDefault("direction", "asc");
        
        if ("desc".equalsIgnoreCase(direction)) {
            queryWrapper.orderByDesc(field);
        } else {
            queryWrapper.orderByAsc(field);
        }
    }
    
    return queryWrapper;
}
```

## 四、前后端一致性对比

### 4.1 操作符支持对比

| 操作符 | 前端支持 | 后端支持 | 一致性 | 说明 |
|--------|----------|----------|--------|------|
| `eq` | ✅ | ✅ | ✅ | 完全一致 |
| `ne` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `gt` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `ge` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `lt` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `le` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `like` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `left_like` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `right_like` | ✅ (配置) | ✅ | ✅ | 完全一致 |
| `in` | ✅ (数组) | ✅ | ✅ | 完全一致 |
| `between` | ✅ (daterange) | ✅ | ✅ | 完全一致 |
| `isNull` | ❌ | ✅ | ⚠️ | 前端未使用 |
| `isNotNull` | ❌ | ✅ | ⚠️ | 前端未使用 |

**结论**: ✅ **前端使用的所有操作符后端都支持**

### 4.2 参数格式对比

#### 4.2.1 单值查询
**前端**:
```javascript
conditions.push({
  field: field.field,
  operator: operator,
  value: value  // 单个值
})
```

**后端**:
```java
String field = (String) condition.get("field");
String operator = (String) condition.get("operator");
Object value = condition.get("value");

queryWrapper.eq(field, value);  // 直接使用
```

**一致性**: ✅ **完全一致**

---

#### 4.2.2 日期范围查询 (between)
**前端**:
```javascript
if (field.component === 'daterange') {
  value = dateRange.value  // 数组: ["2026-03-01", "2026-03-30"]
  conditions.push({
    field: field.field,
    operator: operator,  // 'between'
    value: value  // 数组格式
  })
}
```

**后端**:
```java
case "between":
    if (value instanceof List && ((List<?>) value).size() >= 2) {
        List<?> values = (List<?>) value;
        queryWrapper.between(field, values.get(0), values.get(1));
    }
    break;
```

**一致性**: ✅ **完全一致**
- 前端传递数组格式
- 后端正确解析数组格式
- 已在 DynamicQueryEngine 中修复支持

---

#### 4.2.3 IN 查询
**前端**:
```javascript
else if (Array.isArray(value)) {
  conditions.push({
    field: field.field,
    operator: operator,  // 'in'
    value: value  // 数组
  })
}
```

**后端**:
```java
case "in":
    if (value instanceof List) {
        queryWrapper.in(field, (List<?>) value);
    }
    break;
```

**一致性**: ✅ **完全一致**

---

#### 4.2.4 排序参数
**前端**:
```javascript
const orderBy = parsedConfig.table?.orderBy || [
  { field: 'FCreateDate', direction: 'DESC' }
]

return {
  conditions: conditions,
  orderBy: orderBy
}
```

**后端**:
```java
List<Map<String, Object>> orderBy = (List<Map<String, Object>>) queryConfig.get("orderBy");
for (Map<String, Object> order : orderBy) {
    String field = (String) order.get("field");
    String direction = (String) order.getOrDefault("direction", "asc");
    
    if ("desc".equalsIgnoreCase(direction)) {
        queryWrapper.orderByDesc(field);
    } else {
        queryWrapper.orderByAsc(field);
    }
}
```

**一致性**: ✅ **完全一致**

## 五、发现的不一致问题

### 5.1 ⚠️ 后端多余的操作符
**问题**: 后端支持 `isNull` 和 `isNotNull` 操作符,但前端未使用

**影响**: 无影响,前端不使用这些操作符

**建议**: 
- 保留这些操作符,供未来扩展使用
- 在前端添加对应的组件支持

---

### 5.2 ⚠️ DynamicQueryEngine 未被使用
**问题**: 
- `DynamicQueryEngine` 已优化,但 `ErpEngineController` 未使用它
- `ErpEngineController` 直接在 `buildQueryFromBuilderMode` 方法中解析参数

**影响**: 
- `DynamicQueryEngine` 的优化未生效
- LIKE 查询未转义特殊字符

**建议**: 
- **方案1**: 在 `ErpEngineController` 中使用 `DynamicQueryEngine`
  ```java
  // 修改 ErpEngineController.java:124行
  queryWrapper = dynamicQueryEngine.buildQueryConditions(
      queryWrapper, queryConfig, queryParams
  );
  ```
  
- **方案2**: 在 `buildQueryFromBuilderMode` 中添加 LIKE 转义
  ```java
  case "like":
      String escapedValue = escapeLikeSpecialChars(value);
      queryWrapper.like(field, escapedValue);
      break;
  
  private String escapeLikeSpecialChars(Object value) {
      if (value == null) return "";
      String strValue = String.valueOf(value);
      return strValue.replace("%", "\\%")
                     .replace("_", "\\_");
  }
  ```

## 六、修复建议

### 6.1 高优先级修复
1. **在 ErpEngineController 中添加 LIKE 转义**
   - 位置: `ErpEngineController.java:1445-1447行`
   - 原因: 防止 SQL 注入
   - 工作量: 0.5 天

### 6.2 中优先级优化
1. **统一使用 DynamicQueryEngine**
   - 位置: `ErpEngineController.java:124行`
   - 原因: 代码复用,统一安全处理
   - 工作量: 1 天

2. **前端添加 isNull/isNotNull 支持**
   - 位置: `BusinessConfigurable.vue`
   - 原因: 功能完整性
   - 工作量: 0.5 天

## 七、测试用例

### 7.1 前端测试
```javascript
// 测试 between 操作符
const dateRange = { value: ['2026-03-01', '2026-03-30'] }
const field = { field: 'FDate', component: 'daterange', queryOperator: 'between' }
const config = buildMainQueryConfig()

expect(config.conditions[0]).toEqual({
  field: 'FDate',
  operator: 'between',
  value: ['2026-03-01', '2026-03-30']
})

// 测试 in 操作符
const queryParams = { status: [1, 2, 3] }
const field = { field: 'status', queryOperator: 'in' }
const config = buildMainQueryConfig()

expect(config.conditions[0]).toEqual({
  field: 'status',
  operator: 'in',
  value: [1, 2, 3]
})
```

### 7.2 后端测试
```java
// 测试 between 操作符
@Test
public void testBetweenOperator() {
    Map<String, Object> condition = Map.of(
        "field", "FDate",
        "operator", "between",
        "value", List.of("2026-03-01", "2026-03-30")
    );
    
    QueryWrapper wrapper = buildQueryFromBuilderMode(
        new QueryWrapper(), 
        Map.of("conditions", List.of(condition))
    );
    
    // 验证 SQL 生成正确
    String sql = wrapper.getCustomSqlSegment();
    assertTrue(sql.contains("BETWEEN"));
}

// 测试 LIKE 转义
@Test
public void testLikeEscape() {
    Map<String, Object> condition = Map.of(
        "field", "name",
        "operator", "like",
        "value", "%"
    );
    
    QueryWrapper wrapper = buildQueryFromBuilderMode(
        new QueryWrapper(), 
        Map.of("conditions", List.of(condition))
    );
    
    // 验证 % 被转义
    String sql = wrapper.getCustomSqlSegment();
    assertTrue(sql.contains("\\%"));
}
```

## 八、总结

### 8.1 一致性评估
| 评估项 | 结果 | 说明 |
|--------|------|------|
| 操作符支持 | ✅ 100% | 前端使用的操作符后端都支持 |
| 参数格式 | ✅ 100% | 前端传递的格式后端都能解析 |
| 数据类型 | ✅ 100% | 前端传递的类型后端都能处理 |
| 安全处理 | ⚠️ 需改进 | LIKE 查询需要转义 |

### 8.2 修复优先级
1. **高优先级**: 添加 LIKE 转义 (安全风险)
2. **中优先级**: 统一使用 DynamicQueryEngine (代码复用)
3. **低优先级**: 前端添加 isNull/isNotNull 支持 (功能扩展)

### 8.3 最终结论
✅ **前后端查询参数基本一致**
⚠️ **需要修复 LIKE 查询安全问题**
✅ **between 操作符已修复,支持数组格式**

修复后,前后端将完全一致,且更安全。
