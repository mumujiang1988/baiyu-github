# 查询参数解析审计报告

## 一、审计概述

**审计时间**: 2024年
**审计范围**: DynamicQueryEngine 查询参数解析逻辑
**审计目标**: 确保所有查询参数都能正确解析,无安全漏洞

## 二、支持的操作符列表

### 2.1 完整支持的操作符

| 操作符 | 说明 | 参数类型 | 代码位置 | 安全性评估 |
|--------|------|----------|----------|------------|
| `like` | 模糊查询 (全匹配) | String | 第67-69行 | ⚠️ **中等风险** |
| `left_like` | 左模糊查询 | String | 第71-73行 | ⚠️ **中等风险** |
| `right_like` | 右模糊查询 | String | 第75-77行 | ⚠️ **中等风险** |
| `in` | IN 查询 | Collection/Array | 第79-85行 | ✅ **安全** |
| `between` | 范围查询 | Map/Array/String | 第87-89行 | ✅ **安全** (已修复) |
| `gt` | 大于 | Number/Date | 第91-93行 | ✅ **安全** |
| `ge` | 大于等于 | Number/Date | 第95-97行 | ✅ **安全** |
| `lt` | 小于 | Number/Date | 第99-101行 | ✅ **安全** |
| `le` | 小于等于 | Number/Date | 第103-105行 | ✅ **安全** |
| `ne` | 不等于 | Any | 第107-109行 | ✅ **安全** |
| `eq` (默认) | 精确查询 | Any | 第111-113行 | ✅ **安全** |

### 2.2 操作符详细说明

#### ✅ 安全的操作符

1. **`in` 操作符** (第79-85行)
   ```java
   if (value instanceof java.util.Collection) {
       queryWrapper.in(field, (java.util.Collection<?>) value);
   } else {
       queryWrapper.eq(field, value);
   }
   ```
   - **优点**: 使用 MyBatis-Plus 的参数化查询,自动防注入
   - **支持类型**: Collection, Array
   - **示例**: `{"field": "status", "operator": "in", "value": [1, 2, 3]}`

2. **`between` 操作符** (第87-89行, 已修复)
   ```java
   handleBetweenCondition(queryWrapper, field, value);
   ```
   - **优点**: 支持多种格式,类型安全
   - **支持格式**:
     - Map: `{"start": "2026-03-01", "end": "2026-03-30"}`
     - Array: `["2026-03-01", "2026-03-30"]`
     - String: `"2026-03-01,2026-03-30"`
   - **示例**: `{"field": "FDate", "operator": "between", "value": ["2026-03-01", "2026-03-30"]}`

3. **比较操作符** (`gt`, `ge`, `lt`, `le`, `ne`, `eq`)
   - **优点**: 直接传递 Object 类型,MyBatis-Plus 自动处理类型转换
   - **支持类型**: Number, Date, String
   - **示例**: `{"field": "amount", "operator": "gt", "value": 1000}`

#### ⚠️ 有风险的操作符

1. **`like` 操作符** (第67-69行)
   ```java
   queryWrapper.like(field, value.toString());
   ```
   - **风险**: 用户可以输入 `%` 或 `_` 来操纵查询
   - **示例攻击**: 用户输入 `%` 可以匹配所有记录
   - **修复建议**:
     ```java
     // 方案1: 转义特殊字符
     String escapedValue = value.toString()
         .replace("%", "\\%")
         .replace("_", "\\_");
     queryWrapper.like(field, escapedValue);
     
     // 方案2: 使用 SqlUtils 工具类
     queryWrapper.like(field, SqlUtils.escapeSql(value.toString()));
     ```

2. **`left_like` 和 `right_like` 操作符** (第71-77行)
   - **风险**: 同 `like` 操作符
   - **修复建议**: 同上

## 三、发现的安全问题

### 3.1 SQL 注入风险

#### 问题1: 字段名未校验
**位置**: DynamicQueryEngine.java:58
**代码**:
```java
// 简单校验字段非空即可（配置中已使用数据库字段名）
```

**问题**: 注释提到"简单校验字段非空即可",但实际未做安全校验

**攻击示例**:
```json
{
  "field": "id; DROP TABLE users; --",
  "operator": "eq",
  "value": 1
}
```

**修复建议**:
```java
// 添加字段白名单校验
private static final Set<String> ALLOWED_FIELDS = Set.of(
    "id", "name", "status", "create_time", "update_time", 
    "FDate", "FAmount", "FStatus"
);

private void validateField(String field) {
    if (!ALLOWED_FIELDS.contains(field)) {
        throw new IllegalArgumentException("非法字段名: " + field);
    }
}

// 在第54行后添加
validateField(field);
```

#### 问题2: LIKE 操作符未转义
**位置**: DynamicQueryEngine.java:67-77
**代码**:
```java
case "like":
    queryWrapper.like(field, value.toString());
    break;
```

**问题**: 用户可以输入 `%` 或 `_` 来操纵查询

**攻击示例**:
```json
{
  "field": "name",
  "operator": "like",
  "value": "%"
}
```

**修复建议**:
```java
case "like":
    String escapedValue = escapeLikeSpecialChars(value.toString());
    queryWrapper.like(field, escapedValue);
    break;

// 添加转义方法
private String escapeLikeSpecialChars(String value) {
    return value.replace("%", "\\%")
                .replace("_", "\\_");
}
```

### 3.2 类型转换问题

#### 问题: toString() 直接使用
**位置**: DynamicQueryEngine.java:61, 68, 72, 76
**代码**:
```java
if (value == null || StringUtils.isEmpty(value.toString())) {
    continue;
}
```

**问题**: 
- `toString()` 可能抛出 NullPointerException
- 对于复杂对象,`toString()` 可能返回非预期值

**修复建议**:
```java
// 使用更安全的判空方式
if (value == null) {
    continue;
}

// 对于字符串类型,使用 String.valueOf()
String strValue = String.valueOf(value);
if (StringUtils.isEmpty(strValue)) {
    continue;
}
```

## 四、参数类型支持情况

### 4.1 基本类型
| 类型 | 支持情况 | 示例 |
|------|----------|------|
| String | ✅ 完全支持 | `"value": "test"` |
| Number | ✅ 完全支持 | `"value": 123` |
| Boolean | ✅ 完全支持 | `"value": true` |
| Date | ✅ 完全支持 | `"value": "2026-03-30"` |

### 4.2 复杂类型
| 类型 | 支持情况 | 示例 |
|------|----------|------|
| Array | ✅ 完全支持 | `"value": [1, 2, 3]` |
| List | ✅ 完全支持 | `"value": ["a", "b", "c"]` |
| Map | ✅ 完全支持 | `"value": {"start": "2026-03-01", "end": "2026-03-30"}` |
| Object | ⚠️ 部分支持 | 依赖 `toString()` 实现 |

### 4.3 特殊场景
| 场景 | 支持情况 | 说明 |
|------|----------|------|
| 空值 | ✅ 正确处理 | 第61-63行跳过空值 |
| null | ✅ 正确处理 | 第61行检查 null |
| 空字符串 | ✅ 正确处理 | 第61行使用 `StringUtils.isEmpty()` |
| 空数组 | ⚠️ 需要测试 | 建议添加测试用例 |
| 空集合 | ⚠️ 需要测试 | 建议添加测试用例 |

## 五、测试建议

### 5.1 单元测试用例

```java
@SpringBootTest
public class DynamicQueryEngineTest {
    
    @Autowired
    private DynamicQueryEngine queryEngine;
    
    // 测试 between 数组格式
    @Test
    public void testBetweenWithArray() {
        QueryWrapper<Entity> wrapper = new QueryWrapper<>();
        Map<String, Object> searchConfig = Map.of(
            "fields", List.of(
                Map.of("field", "FDate", "searchType", "between")
            )
        );
        Map<String, Object> queryParams = Map.of(
            "FDate", List.of("2026-03-01", "2026-03-30")
        );
        
        QueryWrapper<Entity> result = queryEngine.buildQueryConditions(
            wrapper, searchConfig, queryParams
        );
        
        assertNotNull(result);
        // 验证 SQL 生成正确
    }
    
    // 测试 SQL 注入防护
    @Test
    public void testSqlInjection() {
        QueryWrapper<Entity> wrapper = new QueryWrapper<>();
        Map<String, Object> searchConfig = Map.of(
            "fields", List.of(
                Map.of("field", "id; DROP TABLE users; --", "searchType", "eq")
            )
        );
        Map<String, Object> queryParams = Map.of(
            "id; DROP TABLE users; --", 1
        );
        
        assertThrows(IllegalArgumentException.class, () -> {
            queryEngine.buildQueryConditions(wrapper, searchConfig, queryParams);
        });
    }
    
    // 测试 LIKE 转义
    @Test
    public void testLikeEscape() {
        QueryWrapper<Entity> wrapper = new QueryWrapper<>();
        Map<String, Object> searchConfig = Map.of(
            "fields", List.of(
                Map.of("field", "name", "searchType", "like")
            )
        );
        Map<String, Object> queryParams = Map.of(
            "name", "%"
        );
        
        QueryWrapper<Entity> result = queryEngine.buildQueryConditions(
            wrapper, searchConfig, queryParams
        );
        
        // 验证 % 被转义为 \%
        String sql = result.getCustomSqlSegment();
        assertTrue(sql.contains("\\%"));
    }
    
    // 测试 IN 查询
    @Test
    public void testInQuery() {
        QueryWrapper<Entity> wrapper = new QueryWrapper<>();
        Map<String, Object> searchConfig = Map.of(
            "fields", List.of(
                Map.of("field", "status", "searchType", "in")
            )
        );
        Map<String, Object> queryParams = Map.of(
            "status", List.of(1, 2, 3)
        );
        
        QueryWrapper<Entity> result = queryEngine.buildQueryConditions(
            wrapper, searchConfig, queryParams
        );
        
        assertNotNull(result);
    }
}
```

### 5.2 集成测试用例

```java
@SpringBootTest
@AutoConfigureMockMvc
public class QueryIntegrationTest {
    
    @Autowired
    private MockMvc mockMvc;
    
    @Test
    public void testQueryWithBetween() throws Exception {
        String requestBody = """
            {
              "moduleCode": "sale_order",
              "tableName": "t_sale_order",
              "queryConfig": {
                "conditions": [
                  {
                    "field": "FDate",
                    "operator": "between",
                    "value": ["2026-03-01", "2026-03-30"]
                  }
                ]
              },
              "pageNum": 1,
              "pageSize": 10
            }
            """;
        
        mockMvc.perform(post("/erp/engine/query/execute")
                .contentType(MediaType.APPLICATION_JSON)
                .content(requestBody))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200));
    }
}
```

## 六、修复优先级

### 🔴 高优先级 (安全风险)
1. **添加字段白名单校验** - 防止 SQL 注入
2. **转义 LIKE 特殊字符** - 防止查询操纵

### 🟡 中优先级 (健壮性)
1. **改进 toString() 使用** - 避免空指针异常
2. **添加空集合处理** - 提高健壮性

### 🟢 低优先级 (优化)
1. **添加更多单元测试** - 提高测试覆盖率
2. **优化日志输出** - 便于调试

## 七、修复建议代码

### 7.1 完整的修复版本

```java
package com.ruoyi.erp.service.engine;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.ruoyi.common.core.utils.StringUtils;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.util.Map;
import java.util.Set;

/**
 * 动态查询引擎 (安全增强版)
 * 根据 JSON 配置生成查询条件
 * 
 * @author JMH
 * @date 2026-03-22
 */
@Slf4j
@Component("erpDynamicQueryEngine")
public class DynamicQueryEngine {
 
    // 字段白名单 (根据实际业务配置)
    private static final Set<String> ALLOWED_FIELDS = Set.of(
        "id", "name", "status", "create_time", "update_time",
        "FDate", "FAmount", "FStatus", "FOrderNo"
    );
    
    /**
     * 根据配置构建查询条件
     */
    @SuppressWarnings("unchecked")
    public <T> QueryWrapper<T> buildQueryConditions(
            QueryWrapper<T> queryWrapper,
            Map<String, Object> searchConfig,
            Map<String, Object> queryParams) {
        
        if (searchConfig == null || queryParams == null) {
            return queryWrapper;
        }

        try {
            // 获取字段配置
            Object fieldsObj = searchConfig.get("fields");
            if (!(fieldsObj instanceof java.util.List)) {
                return queryWrapper;
            }

            java.util.List<Map<String, Object>> fields = 
                (java.util.List<Map<String, Object>>) fieldsObj;

            // 遍历每个搜索字段配置
            for (Map<String, Object> fieldConfig : fields) {
                String field = (String) fieldConfig.get("field");
                String searchType = (String) fieldConfig.get("searchType");
                
                if (StringUtils.isEmpty(field)) {
                    continue;
                }
                
                // 🔒 安全校验: 字段名白名单
                validateField(field);

                Object value = queryParams.get(field);
                if (value == null) {
                    continue;
                }

                // 根据搜索类型构建查询条件
                switch (StringUtils.defaultString(searchType)) {
                    case "like":
                        queryWrapper.like(field, escapeLikeSpecialChars(value));
                        break;
                    
                    case "left_like":
                        queryWrapper.likeLeft(field, escapeLikeSpecialChars(value));
                        break;
                    
                    case "right_like":
                        queryWrapper.likeRight(field, escapeLikeSpecialChars(value));
                        break;
                    
                    case "in":
                        if (value instanceof java.util.Collection) {
                            queryWrapper.in(field, (java.util.Collection<?>) value);
                        } else {
                            queryWrapper.eq(field, value);
                        }
                        break;
                    
                    case "between":
                        handleBetweenCondition(queryWrapper, field, value);
                        break;
                    
                    case "gt":
                        queryWrapper.gt(field, value);
                        break;
                    
                    case "ge":
                        queryWrapper.ge(field, value);
                        break;
                    
                    case "lt":
                        queryWrapper.lt(field, value);
                        break;
                    
                    case "le":
                        queryWrapper.le(field, value);
                        break;
                    
                    case "ne":
                        queryWrapper.ne(field, value);
                        break;
                    
                    default:
                        // 默认精确查询
                        queryWrapper.eq(field, value);
                }
            }

            log.info("动态查询条件构建成功，字段数：{}", fields.size());

        } catch (Exception e) {
            log.error("动态查询条件构建失败", e);
            throw new RuntimeException("查询条件构建失败: " + e.getMessage(), e);
        }

        return queryWrapper;
    }
    
    /**
     * 校验字段名是否在白名单中
     */
    private void validateField(String field) {
        if (!ALLOWED_FIELDS.contains(field)) {
            log.warn("非法字段名访问尝试: {}", field);
            throw new IllegalArgumentException("非法字段名: " + field);
        }
    }
    
    /**
     * 转义 LIKE 查询的特殊字符
     */
    private String escapeLikeSpecialChars(Object value) {
        if (value == null) {
            return "";
        }
        String strValue = String.valueOf(value);
        return strValue.replace("%", "\\%")
                       .replace("_", "\\_");
    }

    /**
     * 处理 Between 查询条件
     */
    @SuppressWarnings("unchecked")
    private <T> void handleBetweenCondition(
            QueryWrapper<T> queryWrapper,
            String field, 
            Object value) {
        
        if (value instanceof Map) {
            Map<String, Object> range = (Map<String, Object>) value;
            Object start = range.get("start");
            Object end = range.get("end");
            
            if (start != null && end != null) {
                try {
                    queryWrapper.between(field, start, end);
                    log.debug("Between条件构建成功(Map格式): field={}, start={}, end={}", 
                        field, start, end);
                } catch (Exception e) {
                    log.error("Between条件构建失败: field={}, start={}, end={}", 
                        field, start, end, e);
                }
            } else {
                if (start != null) {
                    queryWrapper.ge(field, start);
                }
                if (end != null) {
                    queryWrapper.le(field, end);
                }
            }
        } else if (value instanceof java.util.List) {
            java.util.List<?> list = (java.util.List<?>) value;
            if (list.size() == 2) {
                Object start = list.get(0);
                Object end = list.get(1);
                
                if (start != null && end != null) {
                    try {
                        queryWrapper.between(field, start, end);
                        log.debug("Between条件构建成功(数组格式): field={}, start={}, end={}", 
                            field, start, end);
                    } catch (Exception e) {
                        log.error("Between条件构建失败(数组格式): field={}, start={}, end={}", 
                            field, start, end, e);
                    }
                }
            } else {
                log.warn("Between条件数组长度不等于2: {}", list.size());
            }
        } else if (value instanceof String) {
            String[] parts = value.toString().split(",");
            if (parts.length == 2) {
                queryWrapper.between(field, parts[0], parts[1]);
            }
        }
    }

    /**
     * 解析排序配置
     */
    public <T> void applySortConfig(
            QueryWrapper<T> queryWrapper,
            Map<String, Object> sortConfig) {
        
        if (sortConfig == null) {
            return;
        }

        String orderBy = (String) sortConfig.get("orderBy");
        String orderDirection = (String) sortConfig.get("orderDirection");

        if (StringUtils.isNotEmpty(orderBy)) {
            // 🔒 安全校验: 排序字段白名单
            validateField(orderBy);
            
            if ("asc".equalsIgnoreCase(orderDirection)) {
                queryWrapper.orderByAsc(true, orderBy);
            } else {
                queryWrapper.orderByDesc(true, orderBy);
            }
        }
    }
}
```

## 八、总结

### 8.1 当前状态
- ✅ **已修复**: `between` 操作符支持数组格式
- ⚠️ **存在风险**: SQL 注入风险 (字段名未校验, LIKE 未转义)
- ✅ **基本完善**: 支持所有常用查询操作符

### 8.2 修复后状态
- ✅ **安全**: 添加字段白名单校验
- ✅ **安全**: 转义 LIKE 特殊字符
- ✅ **健壮**: 改进空值处理
- ✅ **完整**: 支持所有查询场景

### 8.3 建议行动
1. **立即修复**: SQL 注入风险 (高优先级)
2. **添加测试**: 单元测试和集成测试
3. **更新文档**: 说明字段白名单配置方法
