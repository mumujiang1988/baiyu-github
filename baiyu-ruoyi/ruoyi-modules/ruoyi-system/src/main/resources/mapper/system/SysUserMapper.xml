<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysUserMapper">

    <resultMap type="com.ruoyi.system.domain.vo.SysUserVo" id="SysUserResult">
        <id property="userId" column="user_id"/>
    </resultMap>

    <resultMap type="com.ruoyi.system.domain.vo.SysUserExportVo" id="SysUserExportResult">
        <id property="userId" column="user_id"/>
    </resultMap>

    <select id="selectPageUserList" resultMap="SysUserResult">
        select u.user_id, u.dept_id, u.nick_name, u.user_name, u.email, u.avatar, u.phonenumber, u.sex,
        u.status, u.del_flag, u.login_ip, u.login_date, u.create_by, u.create_time, u.remark,u.k3_key,u.staff_id
        from sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        ${ew.getCustomSqlSegment}
    </select>

    <select id="selectUserList" resultMap="SysUserResult">
        select u.user_id, u.dept_id, u.nick_name, u.user_name, u.email, u.avatar, u.phonenumber, u.sex,
            u.status, u.del_flag, u.login_ip, u.login_date, u.create_by, u.create_time, u.remark
        from sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        ${ew.getCustomSqlSegment}
    </select>

    <select id="selectAllocatedList" resultMap="SysUserResult">
        select distinct u.user_id, u.dept_id, u.user_name, u.nick_name, u.email, u.phonenumber, u.status, u.create_time
        from sys_user u
             left join sys_dept d on u.dept_id = d.dept_id
             left join sys_user_role sur on u.user_id = sur.user_id
             left join sys_role r on r.role_id = sur.role_id
        ${ew.getCustomSqlSegment}
    </select>

    <select id="selectUnallocatedList" resultMap="SysUserResult">
        select distinct u.user_id, u.dept_id, u.user_name, u.nick_name, u.email, u.phonenumber, u.status, u.create_time
        from sys_user u
             left join sys_dept d on u.dept_id = d.dept_id
             left join sys_user_role sur on u.user_id = sur.user_id
             left join sys_role r on r.role_id = sur.role_id
        ${ew.getCustomSqlSegment}
    </select>


    <select id="countUserById" resultType="Long">
        select count(*) from sys_user where del_flag = '0' and user_id = #{userId}
    </select>

    <select id="selectUserExportList" resultMap="SysUserExportResult">
        select u.user_id, u.dept_id, u.nick_name, u.user_name, u.email, u.avatar, u.phonenumber, u.sex,
        u.status, u.del_flag, u.login_ip, u.login_date, u.create_by, u.create_time, u.remark,
        d.dept_name, d.leader, u1.user_name as leaderName
        from sys_user u
        left join sys_dept d on u.dept_id = d.dept_id
        left join sys_user u1 on u1.user_id = d.leader
        ${ew.getCustomSqlSegment}
    </select>

    <select id="selectMapByLoginName" resultType="map">
        SELECT
        k3_key,
        staff_id,
        user_name
        FROM sys_user
        <where>
            <foreach collection="k3key" item="item" separator=" OR ">
                (
                TRIM(LOWER(k3_key)) = TRIM(LOWER(#{item}))
                OR TRIM(LOWER(staff_id)) = TRIM(LOWER(#{item}))
                )
            </foreach>
        </where>
    </select>


    <select id="selectLoginName" parameterType="string" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
            user_id,
            user_name,
                k3_key AS k3Key,
            email,
            status
        FROM sys_user
        WHERE user_name = #{username}
    </select>

    <select id="selectAllLoginNames" resultType="string">
        SELECT
            user_name
        FROM sys_user
        WHERE is_deleted = 0
    </select>
    <select id="selectNickname" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
            k3_key AS k3Key,
            staff_id as staffId,
            user_name,
            nick_name,
            email,
            status
        FROM sys_user
        WHERE user_id = #{userId}
    </select>

    <select id="selectUserByStaffId" parameterType="string" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
            user_id AS userId,
            staff_id AS staffId,
            k3_key AS k3Key,
            user_name AS userName,
            nick_name AS nickName,
            email,
            status
        FROM sys_user
        WHERE staff_id = #{staffId} or nick_name = #{staffId}
        LIMIT 1
    </select>

    <select id="selectUserByK3key" parameterType="string" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
        nick_name AS nickName
        FROM sys_user
        WHERE k3_key = #{k3key}
    </select>


    <select id="selectsStaffIdNames" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
        fname as nickName,fstaff_number as staffId
        FROM sys_employee e
        LEFT JOIN sys_user u on e.fid = u.staff_id
        WHERE status = 1 and dept_id in ('1995775913370284034','1995777305564639233')
    </select>

    <!-- 销售员 -->
    <select id="selectSales" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
        u.nick_name as nickName,
        d.dept_name as departmentName,
        e.salesman_id as fseller,
        GROUP_CONCAT(DISTINCT sr.role_name) as roleNames
        FROM sys_user u
        LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        LEFT JOIN sys_user_role ur ON u.user_id = ur.user_id
        LEFT JOIN sys_role sr ON ur.role_id = sr.role_id
        LEFT JOIN sys_employee e ON u.staff_id = e.fid
        WHERE (
        u.dept_id IN ('1995775271620800513', '1995776039019048962', '1995776549579091969', '1995776618810273794')
        OR ur.role_id IN (1, 2016378335548186625, 2021458108021686273)
        )
        AND u.status = '1'
        AND u.del_flag = '0'
        GROUP BY u.user_id, u.nick_name, u.staff_id, d.dept_name,e.salesman_id
        ORDER BY u.nick_name
    </select>

    <select id="selectsk3keyNames" resultType="com.ruoyi.system.domain.bo.SysUserBo">
        SELECT
            k3_key AS k3Key,
            staff_id AS staffId,
            user_name AS loginName,
            nick_name,
            email,
            status
        FROM sys_user
        WHERE k3_key IS NOT NULL AND account_type=0
    </select>
</mapper>
