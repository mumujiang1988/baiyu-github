# 低代码系统问题修复方案

> **文档版本**: v1.0  
> **创建时间**: 2026-03-25  
> **修复优先级**: P0(高危) > P1(中危) > P2(低危)  
> **预计总工时**: 7人日

---

## 📋 目录

- [一、修复优先级总览](#一修复优先级总览)
- [二、P0高危问题修复](#二p0高危问题修复)
- [三、P1中危问题修复](#三p1中危问题修复)
- [四、P2低危问题修复](#四p2低危问题修复)
- [五、修复验证清单](#五修复验证清单)
- [六、回归测试方案](#六回归测试方案)

---

## 一、修复优先级总览

### 1.1 问题分类

| 优先级 | 问题数量 | 预计工时 | 修复期限 | 状态 |
|--------|---------|---------|---------|------|
| 🔴 P0 高危 | 3个 | 2人日 | 立即修复 | ❌ 待修复 |
| 🟡 P1 中危 | 5个 | 2人日 | 1周内修复 | ❌ 待修复 |
| 🟢 P2 低危 | 3个 | 2人日 | 1月内修复 | ❌ 待修复 |
| 🔵 后期优化 | 2个 | - | 后期规划 | ⏳ 待规划 |

### 1.2 P0高危问题清单

| 编号 | 问题 | 位置 | 预计工时 | 影响 |
|------|------|------|---------|------|
| P0-1 | DictionaryBuilder.js栈溢出 | DictionaryBuilder.js:266 | 0.5天 | 前端崩溃 |
| P0-2 | 除零异常未捕获 | PushDownEngine.java:294 | 0.5天 | 下推失败 |
| P0-3 | 运算优先级错误 | ComputedFieldEngine.java:311 | 1天 | 计算错误 |

### 1.3 后期优化问题

| 编号 | 问题 | 位置 | 解决方案 | 规划时间 |
|------|------|------|---------|---------|
| L1 | SQL注入-字段白名单删除 | DynamicQueryEngine.java:61 | 控制字段名不可手动输入 | 后期规划 |
| L2 | SQL拼接注入 | VirtualFieldService.java:174 | 控制表名和字段名不可手动输入 | 后期规划 |

---

## 二、P0高危问题修复

### 2.1 P0-1: DictionaryBuilder.js栈溢出修复

**问题描述**: get方法重复定义导致无限递归,栈溢出崩溃

**影响范围**: 所有使用字典的前端页面

**修复方案**: 重命名第二个get方法为getData

#### 修复前代码

```javascript
// DictionaryBuilder.js:224-230
/**
 * 获取字典构建器
 */
get(name) { // 第一次定义
  const builder = this.registry.get(name)
  if (!builder) {
    console.warn(`⚠️ 字典未注册：${name}`)
  }
  return builder
}

// DictionaryBuilder.js:266-272
/**
 * 获取字典数据
 */
get(name) { // ❌❌ 第二次定义,覆盖了第一个
  const builder = this.get(name) // ❌❌❌ 递归调用自己,导致栈溢出
  if (!builder) {
    return []
  }
  return builder.get()
}
```

#### 修复后代码

```javascript
// DictionaryBuilder.js:224-230 (保持不变)
/**
 * 获取字典构建器
 * @param {string} name - 字典名称
 * @returns {DictionaryBuilder|null}
 */
get(name) {
  const builder = this.registry.get(name)
  if (!builder) {
    console.warn(`⚠️ 字典未注册：${name}`)
  }
  return builder
}

// DictionaryBuilder.js:266-272 (修改)
/**
 * 获取字典数据(直接从缓存读取)
 * @param {string} name - 字典名称
 * @returns {Array}
 */
getData(name) { // ✅ 重命名为getData
  const builder = this.get(name) // ✅ 调用第一个get方法
  if (!builder) {
    return []
  }
  return builder.get()
}
```

#### 需要同步修改的调用方

```javascript
// 所有调用engine.get(name)获取数据的地方,改为engine.getData(name)

// 示例1: BusinessConfigurable.vue
// 修改前
const options = dictionaryEngine.get('salespersons')

// 修改后
const options = dictionaryEngine.getData('salespersons')

// 示例2: saleorder.vue
// 修改前
const customerOptions = this.dictionaryEngine.get('customers')

// 修改后
const customerOptions = this.dictionaryEngine.getData('customers')
```

#### 验证方法

```javascript
// 单元测试
describe('DictionaryBuilderEngine', () => {
  it('should not cause stack overflow when calling getData method', () => {
    const engine = new DictionaryBuilderEngine()
    const builder = DictionaryBuilder.buildStatic('test', [
      { label: '选项1', value: '1' },
      { label: '选项2', value: '2' }
    ])
    
    engine.register('test', builder)
    
    // 调用getData方法不应导致栈溢出
    const result = engine.getData('test')
    expect(result).toEqual([
      { label: '选项1', value: '1' },
      { label: '选项2', value: '2' }
    ])
  })
  
  it('should return builder when calling get method', () => {
    const engine = new DictionaryBuilderEngine()
    const builder = DictionaryBuilder.buildStatic('test', [])
    
    engine.register('test', builder)
    
    // 调用get方法应返回builder对象
    const result = engine.get('test')
    expect(result).toBeInstanceOf(DictionaryBuilder)
  })
})
```

---

### 2.2 P0-2: 除零异常修复

**问题描述**: SimpleCalculator除法运算未检查除数是否为0

**影响范围**: 所有使用下推引擎的业务操作

**修复方案**: 添加除数检查

#### 修复前代码

```java
// PushDownEngine.java:290-297
private double parseTerm() {
    double x = parseFactor();
    for (;;) {
        if (eat('*')) x *= parseFactor();
        else if (eat('/')) x /= parseFactor(); // ❌ 可能除零
        else return x;
    }
}
```

#### 修复后代码

```java
// PushDownEngine.java:290-300
private double parseTerm() {
    double x = parseFactor();
    for (;;) {
        if (eat('*')) {
            x *= parseFactor();
        } else if (eat('/')) {
            double divisor = parseFactor();
            // ✅ 检查除数是否为0
            if (Math.abs(divisor) < 1e-10) { // 浮点数精度问题,使用极小值判断
                throw new ArithmeticException("除数不能为零");
            }
            x /= divisor;
        } else {
            return x;
        }
    }
}
```

#### 增强异常处理

```java
// PushDownEngine.java:239-248
/**
 * 计算数学表达式
 */
private Object calculateExpression(String expression) {
    try {
        // 简单的四则运算计算器
        // 支持加减乘除
        return new SimpleCalculator().calculate(expression);
    } catch (ArithmeticException e) {
        // ✅ 捕获除零异常,返回友好的错误信息
        log.error("表达式计算失败(除零错误): {}", expression, e);
        throw new RuntimeException("计算错误: " + e.getMessage(), e);
    } catch (Exception e) {
        log.error("表达式计算失败: {}", expression, e);
        return null;
    }
}
```

#### 验证方法

```java
// 单元测试
@Test
public void testCalculateExpression_DivideByZero() {
    PushDownEngine engine = new PushDownEngine();
    
    // 测试除零异常
    assertThrows(ArithmeticException.class, () -> {
        engine.calculateExpression("10 / 0");
    });
    
    // 测试除以极小值
    assertThrows(ArithmeticException.class, () -> {
        engine.calculateExpression("10 / 0.0000000001");
    });
}

@Test
public void testCalculateExpression_NormalDivision() {
    PushDownEngine engine = new PushDownEngine();
    
    // 测试正常除法
    double result = engine.calculateExpression("10 / 2");
    assertThat(result).isEqualTo(5.0, within(0.001));
    
    // 测试复杂表达式
    result = engine.calculateExpression("(100 + 50) / 3");
    assertThat(result).isEqualTo(50.0, within(0.001));
}
```

---

### 2.3 P0-3: 运算优先级错误修复

**问题描述**: 表达式计算器运算符优先级处理错误

**影响范围**: 所有使用计算字段的业务场景

**修复方案**: 重写表达式计算器,正确处理运算符优先级

#### 方案1: 修复现有计算器(推荐)

```java
// ComputedFieldEngine.java:307-380 (重写)
/**
 * 简单四则运算计算器(修复运算符优先级)
 * 优先级: 括号 > 乘除 > 加减
 */
private BigDecimal calculateSimpleExpression(String expression) {
    // 移除空格
    expression = expression.replaceAll("\\s+", "");
    
    // 处理括号(最高优先级)
    int parenStart = expression.lastIndexOf('(');
    if (parenStart >= 0) {
        int parenEnd = expression.indexOf(')', parenStart);
        if (parenEnd < 0) {
            throw new IllegalArgumentException("括号不匹配");
        }
        
        // 递归计算括号内的表达式
        String before = expression.substring(0, parenStart);
        String inside = expression.substring(parenStart + 1, parenEnd);
        String after = expression.substring(parenEnd + 1);
        
        BigDecimal insideValue = calculateSimpleExpression(inside);
        return calculateSimpleExpression(before + insideValue + after);
    }
    
    // 处理加减法(最低优先级,从右往左找)
    int plusIndex = findOperatorIndex(expression, '+');
    if (plusIndex > 0) {
        BigDecimal left = calculateSimpleExpression(expression.substring(0, plusIndex));
        BigDecimal right = calculateSimpleExpression(expression.substring(plusIndex + 1));
        return left.add(right);
    }
    
    int minusIndex = findOperatorIndex(expression, '-');
    if (minusIndex > 0) {
        // 处理负数情况: -5 或 3*-5
        if (minusIndex == 0) {
            // 整个表达式是负数
            return calculateSimpleExpression(expression.substring(1)).negate();
        }
        char prevChar = expression.charAt(minusIndex - 1);
        if (prevChar == '+' || prevChar == '-' || prevChar == '*' || prevChar == '/') {
            // 负号,不是减号,继续找下一个减号
            int nextMinus = findOperatorIndex(expression.substring(0, minusIndex), '-');
            if (nextMinus > 0) {
                minusIndex = nextMinus;
            } else {
                // 整个是负数表达式
                return calculateSimpleExpression(expression.substring(1)).negate();
            }
        }
        
        BigDecimal left = calculateSimpleExpression(expression.substring(0, minusIndex));
        BigDecimal right = calculateSimpleExpression(expression.substring(minusIndex + 1));
        return left.subtract(right);
    }
    
    // 处理乘除法(高优先级)
    int multiplyIndex = expression.indexOf('*');
    int divideIndex = expression.indexOf('/');
    
    // 优先处理先出现的运算符(从左往右)
    if (multiplyIndex > 0 && (divideIndex < 0 || multiplyIndex < divideIndex)) {
        BigDecimal left = calculateSimpleExpression(expression.substring(0, multiplyIndex));
        BigDecimal right = calculateSimpleExpression(expression.substring(multiplyIndex + 1));
        return left.multiply(right);
    }
    
    if (divideIndex > 0) {
        BigDecimal left = calculateSimpleExpression(expression.substring(0, divideIndex));
        BigDecimal right = calculateSimpleExpression(expression.substring(divideIndex + 1));
        if (right.compareTo(BigDecimal.ZERO) == 0) {
            throw new ArithmeticException("除数不能为零");
        }
        return left.divide(right, 10, RoundingMode.HALF_UP);
    }
    
    // 解析数字
    try {
        return new BigDecimal(expression);
    } catch (NumberFormatException e) {
        throw new IllegalArgumentException("无法解析数字：" + expression);
    }
}

/**
 * 查找运算符索引(忽略括号内的运算符)
 * 从右往左查找,确保正确的运算顺序
 */
private int findOperatorIndex(String expression, char operator) {
    int parenDepth = 0;
    for (int i = expression.length() - 1; i >= 0; i--) {
        char c = expression.charAt(i);
        if (c == ')') {
            parenDepth++;
        } else if (c == '(') {
            parenDepth--;
        } else if (c == operator && parenDepth == 0) {
            // 确保不是负号(前面是运算符或开头)
            if (operator == '-' && i > 0) {
                char prev = expression.charAt(i - 1);
                if (prev == '+' || prev == '-' || prev == '*' || prev == '/') {
                    continue;
                }
            }
            return i;
        }
    }
    return -1;
}
```

#### 方案2: 引入Aviator表达式引擎(备选)

```xml
<!-- pom.xml 添加依赖 -->
<dependency>
    <groupId>com.googlecode.aviator</groupId>
    <artifactId>aviator</artifactId>
    <version>5.3.3</version>
</dependency>
```

```java
// ComputedFieldEngine.java
import com.googlecode.aviator.AviatorEvaluator;
import com.googlecode.aviator.Expression;

/**
 * 使用Aviator计算表达式
 */
private Object evaluateExpression(String expression, Map<String, Object> context) {
    try {
        // 替换变量为实际值
        String evaluatedExpr = expression;
        for (Map.Entry<String, Object> entry : context.entrySet()) {
            String key = entry.getKey();
            Object value = entry.getValue();
            if (value instanceof Number) {
                evaluatedExpr = evaluatedExpr.replaceAll("\\b" + key + "\\b", value.toString());
            }
        }
        
        // 使用Aviator计算
        Object result = AviatorEvaluator.execute(evaluatedExpr);
        
        // 转换为BigDecimal
        if (result instanceof Number) {
            return new BigDecimal(result.toString());
        }
        
        return result;
    } catch (Exception e) {
        throw new RuntimeException("表达式计算失败：" + expression, e);
    }
}
```

#### 验证方法

```java
// 单元测试
@Test
public void testCalculateExpression_OperatorPrecedence() {
    ComputedFieldEngine engine = new ComputedFieldEngine();
    
    // 测试运算优先级: 2 + 3 * 4 = 14 (不是20)
    BigDecimal result = engine.calculateSimpleExpression("2 + 3 * 4");
    assertThat(result).isEqualByComparingTo("14");
    
    // 测试括号优先级: (2 + 3) * 4 = 20
    result = engine.calculateSimpleExpression("(2 + 3) * 4");
    assertThat(result).isEqualByComparingTo("20");
    
    // 测试复杂表达式: 10 - 2 * 3 + 4 / 2 = 10 - 6 + 2 = 6
    result = engine.calculateSimpleExpression("10 - 2 * 3 + 4 / 2");
    assertThat(result).isEqualByComparingTo("6");
    
    // 测试嵌套括号: ((2 + 3) * 4 - 5) / 3 = (20 - 5) / 3 = 5
    result = engine.calculateSimpleExpression("((2 + 3) * 4 - 5) / 3");
    assertThat(result).isEqualByComparingTo("5");
}

@Test
public void testCalculateExpression_NegativeNumbers() {
    ComputedFieldEngine engine = new ComputedFieldEngine();
    
    // 测试负数: -5 + 3 = -2
    BigDecimal result = engine.calculateSimpleExpression("-5 + 3");
    assertThat(result).isEqualByComparingTo("-2");
    
    // 测试负数乘法: 3 * -4 = -12
    result = engine.calculateSimpleExpression("3 * -4");
    assertThat(result).isEqualByComparingTo("-12");
}
```

---

## 三、P1中危问题修复

### 3.1 P1-1: 正则表达式重复编译优化

**修复方案**: 将Pattern编译为静态常量

```java
// PushDownEngine.java:20-30
package com.ruoyi.erp.service.engine;

import com.ruoyi.common.core.utils.StringUtils;
import lombok.Data;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.math.BigDecimal;
import java.util.*;
import java.util.regex.Pattern; // ✅ 导入Pattern

/**
 * 下推引擎(性能优化版)
 */
@Slf4j
@Component
public class PushDownEngine {
    
    /**
     * ✅ 正则表达式编译为静态常量,避免重复编译
     */
    private static final Pattern VARIABLE_PATTERN = 
        Pattern.compile("\\b([a-zA-Z_][a-zA-Z0-9_]*)\\b");

    // ... 其他代码 ...

    /**
     * 简单公式解析(性能优化版)
     */
    private Object evaluateSimpleFormula(String formula, Map<String, Object> context) {
        // 替换变量为实际值
        String expression = formula;
        
        // ✅ 使用静态Pattern
        java.util.regex.Matcher matcher = VARIABLE_PATTERN.matcher(formula);
        
        StringBuffer sb = new StringBuffer();
        while (matcher.find()) {
            String varName = matcher.group(1);
            Object value = context.get(varName);
            
            if (value != null) {
                if (value instanceof Number) {
                    matcher.appendReplacement(sb, value.toString());
                } else {
                    try {
                        double numValue = Double.parseDouble(value.toString());
                        matcher.appendReplacement(sb, String.valueOf(numValue));
                    } catch (NumberFormatException e) {
                        log.warn("变量 {} 的值不是数值: {}", varName, value);
                        matcher.appendReplacement(sb, "0");
                    }
                }
            } else {
                matcher.appendReplacement(sb, "0");
            }
        }
        matcher.appendTail(sb);
        expression = sb.toString();

        return calculateExpression(expression);
    }
}
```

---

### 3.2 P1-2: 缓存容量限制优化

**修复方案**: 使用Caffeine Cache

```xml
<!-- pom.xml 添加依赖 -->
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
    <version>3.1.8</version>
</dependency>
```

```java
// DictionaryBuilderEngine.java:20-30
package com.ruoyi.erp.service.engine;

import com.github.benmanes.caffeine.cache.Cache;
import com.github.benmanes.caffeine.cache.Caffeine;
import com.ruoyi.common.core.utils.StringUtils;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.util.*;
import java.util.concurrent.TimeUnit;

/**
 * 字典构建器引擎(性能优化版)
 */
@Slf4j
@Component
public class DictionaryBuilderEngine {
    
    /**
     * ✅ 使用Caffeine Cache,设置容量限制
     */
    private final Cache<String, DictionaryCache> dictionaryCache = 
        Caffeine.newBuilder()
            .maximumSize(1000) // 最多缓存1000个字典
            .expireAfterWrite(5, TimeUnit.MINUTES) // 5分钟过期
            .recordStats() // 记录统计信息
            .build();
    
    // 默认缓存时间：5 分钟
    private static final long DEFAULT_TTL = 5 * 60 * 1000;
    
    /**
     * 获取缓存统计信息
     */
    public Map<String, Object> getCacheStats() {
        var stats = dictionaryCache.stats();
        return Map.of(
            "hitCount", stats.hitCount(),
            "missCount", stats.missCount(),
            "hitRate", stats.hitRate(),
            "evictionCount", stats.evictionCount(),
            "size", dictionaryCache.estimatedSize()
        );
    }
    
    // ... 其他方法修改为使用Cache API ...
    
    /**
     * 构建静态字典
     */
    public DictionaryBuilderEngine buildStatic(String name, List<Map<String, Object>> options) {
        dictionaryCache.put(name, new DictionaryCache(options, Long.MAX_VALUE));
        log.info("✅ 构建静态字典：{}, 共 {} 条", name, options.size());
        return this;
    }
    
    /**
     * 获取字典数据
     */
    public List<Map<String, Object>> get(String name) {
        DictionaryCache cache = dictionaryCache.getIfPresent(name);
        if (cache == null || cache.data == null) {
            return new ArrayList<>();
        }
        return cache.data;
    }
    
    /**
     * 清除字典缓存
     */
    public void clear(String name) {
        dictionaryCache.invalidate(name);
        log.info("🗑️ 已清除字典缓存：{}", name);
    }
    
    /**
     * 清除所有字典缓存
     */
    public void clearAll() {
        dictionaryCache.invalidateAll();
        log.info("🗑️ 已清除所有字典缓存");
    }
}
```

---

### 3.3 P1-3: XSS防护修复

**修复方案**: 添加HTML转义

```javascript
// multiTableQueryBuilder.js:1-20 (新增转义函数)
/**
 * HTML转义函数,防止XSS攻击
 * @param {string} unsafe - 不安全的字符串
 * @returns {string} - 转义后的安全字符串
 */
const escapeHtml = (unsafe) => {
  if (typeof unsafe !== 'string') {
    return unsafe
  }
  return unsafe
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;")
}

/**
 * 替换查询条件中的模板变量(安全版)
 * @param {Array} conditions - 查询条件数组
 * @param {Object} contextData - 上下文数据
 * @returns {Array} - 替换后的条件数组
 */
export const replaceTemplateVariables = (conditions, contextData) => {
  if (!Array.isArray(conditions)) {
    return conditions
  }
  
  return conditions.map(condition => {
    let value = condition.value
    
    // 处理数组类型的值
    if (Array.isArray(value)) {
      value = value.map(v => {
        if (typeof v === 'string' && v.startsWith('${') && v.endsWith('}')) {
          const varName = v.slice(2, -1)
          const rawValue = contextData[varName] || v
          // ✅ 对替换后的值进行HTML转义
          return escapeHtml(rawValue)
        }
        return v
      })
    } 
    // 处理字符串类型的值
    else if (typeof value === 'string' && value.startsWith('${') && value.endsWith('}')) {
      const varName = value.slice(2, -1)
      const rawValue = contextData[varName] || value
      // ✅ 对替换后的值进行HTML转义
      value = escapeHtml(rawValue)
    }
    
    return {
      ...condition,
      value
    }
  })
}
```

---

### 3.4 P1-4: 聚合函数重复代码重构

**修复方案**: 提取通用方法

```java
// ComputedFieldEngine.java:110-150 (重构)
/**
 * 提取聚合函数的字段值列表
 * 通用方法,消除重复代码
 */
private List<BigDecimal> extractAggregateValues(
        String formula, 
        String functionName,
        Map<String, Object> context) {
    
    // 提取字段路径：SUM(entryList.fAllAmount)
    int funcNameLength = functionName.length();
    String fieldPath = formula.substring(funcNameLength + 1, formula.length() - 1);
    String[] parts = fieldPath.split("\\.");
    
    if (parts.length != 2) {
        throw new IllegalArgumentException(
            functionName + " 函数格式错误，应为：" + functionName + "(list.field)"
        );
    }
    
    String listField = parts[0];
    String valueField = parts[1];
    
    Object listObj = context.get(listField);
    if (!(listObj instanceof List)) {
        return Collections.emptyList();
    }
    
    List<?> list = (List<?>) listObj;
    if (CollUtil.isEmpty(list)) {
        return Collections.emptyList();
    }
    
    List<BigDecimal> values = new ArrayList<>();
    for (Object item : list) {
        if (item instanceof Map) {
            Object value = ((Map<?, ?>) item).get(valueField);
            if (value instanceof Number) {
                values.add(new BigDecimal(value.toString()));
            }
        }
    }
    
    return values;
}

/**
 * SUM 函数计算
 */
private Object evaluateSum(String formula, Map<String, Object> context) {
    List<BigDecimal> values = extractAggregateValues(formula, "SUM", context);
    return values.stream()
        .reduce(BigDecimal.ZERO, BigDecimal::add);
}

/**
 * AVG 函数计算
 */
private Object evaluateAvg(String formula, Map<String, Object> context) {
    List<BigDecimal> values = extractAggregateValues(formula, "AVG", context);
    if (values.isEmpty()) {
        return BigDecimal.ZERO;
    }
    
    BigDecimal sum = values.stream()
        .reduce(BigDecimal.ZERO, BigDecimal::add);
    
    return sum.divide(
        BigDecimal.valueOf(values.size()), 
        10, 
        RoundingMode.HALF_UP
    );
}

/**
 * MAX 函数计算
 */
private Object evaluateMax(String formula, Map<String, Object> context) {
    List<BigDecimal> values = extractAggregateValues(formula, "MAX", context);
    return values.stream()
        .max(BigDecimal::compareTo)
        .orElse(null);
}

/**
 * MIN 函数计算
 */
private Object evaluateMin(String formula, Map<String, Object> context) {
    List<BigDecimal> values = extractAggregateValues(formula, "MIN", context);
    return values.stream()
        .min(BigDecimal::compareTo)
        .orElse(null);
}
```

---

### 3.5 P1-5: 配置解析容错性优化

**修复方案**: 使用策略模式

```javascript
// ERPConfigParser.js:1-50 (重构)
/**
 * 配置解析策略接口
 */
class ConfigParseStrategy {
  canParse(data) {
    throw new Error('子类必须实现canParse方法')
  }
  
  parse(data) {
    throw new Error('子类必须实现parse方法')
  }
}

/**
 * 字符串配置解析策略
 */
class StringConfigParseStrategy extends ConfigParseStrategy {
  canParse(data) {
    return typeof data === 'string'
  }
  
  parse(data) {
    console.log('📝 rawData 是字符串,长度:', data.length)
    try {
      const config = JSON.parse(data)
      console.log('✅ JSON 解析成功')
      return config
    } catch (parseError) {
      console.error('❌ JSON 解析失败:', parseError, '原始数据:', data)
      throw new Error('配置内容格式错误')
    }
  }
}

/**
 * 对象配置解析策略
 */
class ObjectConfigParseStrategy extends ConfigParseStrategy {
  canParse(data) {
    return data && typeof data === 'object'
  }
  
  parse(data) {
    console.log('📝 rawData 已经是对象')
    return data
  }
}

/**
 * 配置解析器(策略模式版)
 */
class ERPConfigParser {
  constructor() {
    // 注册解析策略
    this.strategies = [
      new StringConfigParseStrategy(),
      new ObjectConfigParseStrategy()
    ]
  }
  
  /**
   * 从数据库加载配置(重构版)
   */
  static async loadFromDatabase(moduleCode) {
    const cacheKey = `erp_config_${moduleCode}`
    
    // 检查缓存
    const cached = configCache.get(cacheKey)
    if (cached && Date.now() - cached.timestamp < CACHE_TTL) {
      console.log('💾 命中缓存配置:', moduleCode)
      return cached.config
    }
    
    try {
      console.log('🌐 从数据库加载配置:', moduleCode)
      const response = await request({
        url: `/erp/config/get/${moduleCode}`,
        method: 'get'
      })
      
      if (response.code === 200 || response.code === 0) {
        let rawData = response.data
        if (!rawData && response.msg) {
          console.warn('⚠️ response.data 为空,尝试从 msg 字段读取配置...')
          rawData = response.msg
        }
        
        if (!rawData) {
          console.error('❌ response.data 和 response.msg 都为空!', response)
          throw new Error('配置内容为空')
        }
        
        // ✅ 使用策略模式解析
        const parser = new ERPConfigParser()
        const configContent = parser.parseConfig(rawData)
        
        // 更新缓存
        configCache.set(cacheKey, {
          config: configContent,
          timestamp: Date.now()
        })
        
        console.log('✅ 数据库配置加载成功:', configContent.pageConfig?.title)
        return configContent
      } else {
        console.error('❌ 后端返回错误码:', response.code, '错误信息:', response.msg)
        throw new Error(response.msg || '配置加载失败')
      }
    } catch (error) {
      console.error('❌ 加载数据库配置失败:', error)
      throw error
    }
  }
  
  /**
   * 解析配置(策略模式)
   */
  parseConfig(data) {
    // 遍历所有策略,找到能处理的策略
    for (const strategy of this.strategies) {
      if (strategy.canParse(data)) {
        return strategy.parse(data)
      }
    }
    
    // 没有策略能处理
    console.error('❌ rawData 类型异常:', typeof data, '值:', data)
    throw new Error('配置内容为空或格式不正确')
  }
}
```

---

## 四、P2低危问题修复

### 4.1 P2-1: 硬编码表名优化

**修复方案**: 从配置中读取

```javascript
// multiTableQueryBuilder.js:210-240 (重构)
/**
 * 根据单据编号查询明细表(配置驱动版)
 * @param {String} moduleCode - 模块编码
 * @param {String} billNo - 单据编号
 * @param {Object} config - 页面配置
 * @returns {Promise<Array>} - 明细数据
 */
export const queryEntryByBillNo = async (moduleCode, billNo, config) => {
  // ✅ 从配置中读取表名和字段名
  const entryConfig = config?.subTableQueryConfigs?.entry
  
  if (!entryConfig) {
    console.warn('⚠️ 未找到明细表配置')
    return []
  }
  
  const queryConfig = {
    conditions: entryConfig.defaultConditions.map(condition => ({
      ...condition,
      value: condition.value === '${billNo}' ? billNo : condition.value
    })),
    orderBy: entryConfig.defaultOrderBy || []
  }
  
  const response = await querySubTable({
    moduleCode,
    tableName: entryConfig.tableName, // ✅ 从配置读取
    queryConfig,
    pageNum: 1,
    pageSize: 100
  })
  
  return response.rows || []
}
```

---

## 五、修复验证清单

### 5.1 P0高危问题验证

| 编号 | 问题 | 验证方法 | 验证结果 |
|------|------|---------|---------|
| P0-1 | DictionaryBuilder.js栈溢出 | 调用getData方法不崩溃 | ❌ 待验证 |
| P0-2 | 除零异常 | 除零抛出友好异常 | ❌ 待验证 |
| P0-3 | 运算优先级 | 2+3*4=14 | ❌ 待验证 |

### 5.2 自动化测试脚本

```bash
#!/bin/bash
# 修复验证脚本

echo "========================================="
echo "低代码系统修复验证脚本"
echo "========================================="

# 1. 运行单元测试
echo "1. 运行单元测试..."
mvn test -Dtest=DynamicQueryEngineTest
mvn test -Dtest=PushDownEngineTest
mvn test -Dtest=ComputedFieldEngineTest
mvn test -Dtest=VirtualFieldServiceTest

# 2. 运行前端测试
echo "2. 运行前端测试..."
npm run test:unit

# 3. SQL注入测试
echo "3. SQL注入测试..."
sqlmap -u "http://localhost:8080/erp/engine/query/execute" \
  --data='{"moduleCode":"saleorder"}' \
  --level=3 --risk=1 --batch

# 4. 性能测试
echo "4. 性能测试..."
jmeter -n -t performance_test.jmx -l results.jtl

echo "========================================="
echo "验证完成"
echo "========================================="
```

---

## 六、回归测试方案

### 6.1 测试范围

| 模块 | 测试类型 | 测试用例数 | 优先级 |
|------|---------|-----------|--------|
| DynamicQueryEngine | 单元测试 | 20 | 🔴 P0 |
| PushDownEngine | 单元测试 | 15 | 🔴 P0 |
| ComputedFieldEngine | 单元测试 | 25 | 🔴 P0 |
| VirtualFieldService | 单元测试 | 10 | 🔴 P0 |
| DictionaryBuilder.js | 单元测试 | 15 | 🔴 P0 |
| 销售订单模块 | 集成测试 | 30 | 🟡 P1 |
| 下推功能 | E2E测试 | 10 | 🟡 P1 |

### 6.2 测试环境

- **测试环境**: http://test.example.com
- **数据库**: MySQL 8.0 (测试库)
- **浏览器**: Chrome、Edge、Firefox
- **测试数据**: 1000条销售订单

### 6.3 测试通过标准

- ✅ 单元测试通过率 > 95%
- ✅ 集成测试通过率 > 90%
- ✅ E2E测试通过率 > 85%
- ✅ 性能测试: TPS > 500, RT < 1s
- ✅ 安全测试: 无高危漏洞

---

## 附录

### A. 修复时间估算

| 阶段 | 任务 | 预计工时 | 负责人 |
|------|------|---------|--------|
| 第1天 | P0-1, P0-2修复 | 1天 | 前端+后端开发 |
| 第2天 | P0-3修复(运算优先级) + 单元测试 | 1天 | 后端开发 |
| 第3天 | P1问题修复 | 1天 | 全员 |
| 第4天 | 回归测试 | 1天 | 测试人员 |
| 第5-6天 | 灰度发布准备 | 2天 | 运维人员 |

**总计**: 6人日

---

### C. 后期优化方案

#### L1: SQL注入-字段白名单问题

**问题描述**: DynamicQueryEngine字段白名单被删除,存在SQL注入风险

**后期解决方案**: 控制字段名不可手动输入

**实现思路**:
1. **前端控制**: 配置界面字段名使用下拉选择,不允许手动输入
2. **后端校验**: 从数据库元数据获取合法字段列表,动态校验
3. **配置加密**: 对配置文件进行签名校验,防止篡改

**实现示例**:

```javascript
// 前端: 字段选择器组件
<template>
  <el-select v-model="fieldValue" placeholder="选择字段">
    <el-option
      v-for="field in allowedFields"
      :key="field.value"
      :label="field.label"
      :value="field.value"
    />
  </el-select>
</template>

<script>
export default {
  data() {
    return {
      // 从后端获取合法字段列表
      allowedFields: [],
      fieldValue: ''
    }
  },
  async mounted() {
    // 从后端获取表结构
    const response = await this.$http.get('/erp/metadata/fields', {
      params: { tableName: 't_sale_order' }
    })
    this.allowedFields = response.data
  }
}
</script>
```

```java
// 后端: 动态字段校验
@RestController
@RequestMapping("/erp/metadata")
public class MetadataController {
    
    @Autowired
    private JdbcTemplate jdbcTemplate;
    
    /**
     * 获取表字段列表(动态白名单)
     */
    @GetMapping("/fields")
    public AjaxResult getTableFields(@RequestParam String tableName) {
        // 1. 校验表名合法性(防止注入)
        if (!isValidTableName(tableName)) {
            return AjaxResult.error("非法表名");
        }
        
        // 2. 查询表结构
        String sql = "SHOW COLUMNS FROM " + tableName;
        List<Map<String, Object>> columns = jdbcTemplate.queryForList(sql);
        
        // 3. 返回字段列表
        List<Map<String, String>> fields = columns.stream()
            .map(col -> Map.of(
                "label", (String) col.get("Field"),
                "value", (String) col.get("Field")
            ))
            .collect(Collectors.toList());
        
        return AjaxResult.success(fields);
    }
    
    /**
     * 校验表名合法性
     */
    private boolean isValidTableName(String tableName) {
        // 只允许字母、数字、下划线
        return tableName.matches("^[a-zA-Z0-9_]+$");
    }
}
```

**优势**:
- ✅ 无需维护硬编码白名单
- ✅ 自动适应表结构变化
- ✅ 前端用户体验好(下拉选择)
- ✅ 安全性高(无法手动输入恶意字段)

**规划时间**: 后期版本迭代

---

#### L2: SQL拼接注入问题

**问题描述**: VirtualFieldService虚拟字段SQL拼接存在注入风险

**后期解决方案**: 控制表名和字段名不可手动输入

**实现思路**:
1. **配置界面**: 虚拟字段配置使用下拉选择表名和字段名
2. **元数据服务**: 从数据库获取合法的表和字段列表
3. **配置校验**: 启动时校验配置文件中的表名和字段名合法性

**实现示例**:

```javascript
// 前端: 虚拟字段配置组件
<template>
  <el-form-item label="源表">
    <el-select v-model="config.sourceTable" @change="handleTableChange">
      <el-option
        v-for="table in allowedTables"
        :key="table.value"
        :label="table.label"
        :value="table.value"
      />
    </el-select>
  </el-form-item>
  
  <el-form-item label="源字段">
    <el-select v-model="config.sourceField">
      <el-option
        v-for="field in tableFields"
        :key="field.value"
        :label="field.label"
        :value="field.value"
      />
    </el-select>
  </el-form-item>
</template>

<script>
export default {
  data() {
    return {
      allowedTables: [], // 从后端获取
      tableFields: [],   // 根据选择的表动态获取
      config: {
        sourceTable: '',
        sourceField: '',
        displayField: ''
      }
    }
  },
  async mounted() {
    // 获取所有表
    const response = await this.$http.get('/erp/metadata/tables')
    this.allowedTables = response.data
  },
  methods: {
    async handleTableChange(tableName) {
      // 根据表名获取字段列表
      const response = await this.$http.get('/erp/metadata/fields', {
        params: { tableName }
      })
      this.tableFields = response.data
    }
  }
}
</script>
```

```java
// 后端: 元数据服务
@RestController
@RequestMapping("/erp/metadata")
public class MetadataController {
    
    @Autowired
    private JdbcTemplate jdbcTemplate;
    
    /**
     * 获取所有表名(动态白名单)
     */
    @GetMapping("/tables")
    public AjaxResult getAllTables() {
        String sql = "SHOW TABLES";
        List<Map<String, Object>> tables = jdbcTemplate.queryForList(sql);
        
        // 过滤出业务表(以t_或bd_开头)
        List<Map<String, String>> result = tables.stream()
            .map(table -> (String) table.values().iterator().next())
            .filter(name -> name.startsWith("t_") || name.startsWith("bd_") || name.startsWith("sys_"))
            .map(name -> Map.of("label", name, "value", name))
            .collect(Collectors.toList());
        
        return AjaxResult.success(result);
    }
    
    /**
     * 获取表字段列表(动态白名单)
     */
    @GetMapping("/fields")
    public AjaxResult getTableFields(@RequestParam String tableName) {
        // 校验表名合法性
        if (!isValidTableName(tableName)) {
            return AjaxResult.error("非法表名");
        }
        
        String sql = "SHOW COLUMNS FROM " + tableName;
        List<Map<String, Object>> columns = jdbcTemplate.queryForList(sql);
        
        List<Map<String, String>> fields = columns.stream()
            .map(col -> Map.of(
                "label", (String) col.get("Field"),
                "value", (String) col.get("Field")
            ))
            .collect(Collectors.toList());
        
        return AjaxResult.success(fields);
    }
    
    /**
     * 校验表名合法性
     */
    private boolean isValidTableName(String tableName) {
        return tableName.matches("^[a-zA-Z0-9_]+$");
    }
}
```

**优势**:
- ✅ 无需维护硬编码白名单
- ✅ 自动适应数据库结构变化
- ✅ 配置界面友好(下拉选择)
- ✅ 安全性高(无法手动输入恶意表名或字段名)

**规划时间**: 后期版本迭代

---

### B. 修复代码仓库

```bash
# 创建修复分支
git checkout -b fix/security-and-logic-issues

# 提交修复代码
git add .
git commit -m "fix: 修复3个P0高危问题

- 修复DictionaryBuilder.js栈溢出问题
- 修复PushDownEngine除零异常
- 修复ComputedFieldEngine运算优先级错误

Closes #1, #2, #3"

# 推送到远程
git push origin fix/security-and-logic-issues

# 创建Pull Request
gh pr create --title "修复安全与逻辑问题" \
  --body "修复3个P0高危问题和5个P1中危问题"
```

---

**文档生成时间**: 2026-03-25  
**文档版本**: v1.0  
**下次更新时间**: 修复完成后

---

> **声明**: 本修复方案基于审计报告生成,实际修复时请根据业务场景和团队技术栈调整。修复完成后请进行充分的回归测试。
