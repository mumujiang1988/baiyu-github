# ERP 配置化方案完整修复方案

> **修复时间**: 2026-03-22  
> **修复人员**: CodeArts代码智能体  
> **修复范围**: P0/P1级别问题完整修复方案  
> **预计工作量**: 5-7人天

---

## 📋 修复任务清单

### P0级别修复(必须立即完成)

- [ ] **修复1**: 实现ErpEngineController的16个TODO
- [ ] **修复2**: ScriptEngine安全加固(使用Aviator引擎)
- [ ] **修复3**: 添加SQL字段白名单校验

### P1级别修复(本周内完成)

- [ ] **修复4**: 补全接口权限注解
- [ ] **修复5**: 添加数据库外键约束和索引
- [ ] **修复6**: 优化缓存键设计

---

## 一、P0级别修复方案

### 1.1 实现ErpEngineController的16个TODO

#### 1.1.1 需要注入的Service

```java
// ErpEngineController.java 添加依赖注入

@RequiredArgsConstructor
@RestController
@RequestMapping("/erp/engine")
public class ErpEngineController extends BaseController {

    private final DynamicQueryEngine queryEngine;
    private final FormValidationEngine validationEngine;
    private final ApprovalWorkflowEngine approvalEngine;
    private final PushDownEngine pushDownEngine;
    
    // 新增注入
    private final ErpApprovalFlowService approvalFlowService;
    private final ErpPushRelationService pushRelationService;
    private final ErpApprovalHistoryMapper approvalHistoryMapper;
    private final ErpPageConfigService pageConfigService;
    
    // 业务Service(根据moduleCode动态获取)
    // 可以使用策略模式或工厂模式
}
```

#### 1.1.2 审批流程TODO实现

**TODO 1: 获取当前审批步骤 (Line 179)**

```java
@PostMapping("/approval/current-step")
public R<?> getCurrentApprovalStep(@RequestBody Map<String, Object> params) {
    try {
        String moduleCode = (String) params.get("moduleCode");
        Object billId = params.get("billId");
        Map<String, Object> billData = (Map<String, Object>) params.get("billData");
        
        // 1. 从数据库获取审批流程配置
        ErpApprovalFlowVo flow = approvalFlowService.getApprovalFlow(moduleCode);
        if (flow == null) {
            return R.fail("未找到审批流程配置");
        }
        
        // 2. 解析流程定义JSON
        Map<String, Object> flowDefinition = JsonUtils.parseMap(flow.getFlowDefinition());
        List<Map<String, Object>> workflow = (List<Map<String, Object>>) flowDefinition.get("workflow");
        
        // 3. 使用审批引擎获取当前步骤
        ApprovalWorkflowEngine.ApprovalStep currentStep = approvalEngine.getCurrentStep(workflow, billData);
        
        // 4. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("step", currentStep);
        result.put("flowId", flow.getFlowId());
        result.put("flowName", flow.getFlowName());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("获取审批步骤失败", e);
        return R.fail("获取审批步骤失败：" + e.getMessage());
    }
}
```

**TODO 2: 执行审批操作 (Line 200)**

```java
@SaCheckPermission("k3:saleOrder:audit")
@PostMapping("/approval/execute")
public R<?> executeApproval(@RequestBody Map<String, Object> params) {
    try {
        Object billId = params.get("billId");
        String moduleCode = (String) params.get("moduleCode");
        String action = (String) params.get("action");
        String opinion = (String) params.get("opinion");
        Integer step = (Integer) params.get("step");
        
        // 1. 获取当前用户信息
        LoginUser loginUser = LoginHelper.getLoginUser();
        String userId = loginUser.getUserId();
        List<String> userRoles = loginUser.getRoles();
        
        // 2. 获取审批流程配置
        ErpApprovalFlowVo flow = approvalFlowService.getApprovalFlow(moduleCode);
        if (flow == null) {
            return R.fail("未找到审批流程配置");
        }
        
        // 3. 解析流程定义
        Map<String, Object> flowDefinition = JsonUtils.parseMap(flow.getFlowDefinition());
        List<Map<String, Object>> workflow = (List<Map<String, Object>>) flowDefinition.get("workflow");
        
        // 4. 获取单据数据(需要根据moduleCode调用对应Service)
        Map<String, Object> billData = getBillData(moduleCode, billId);
        
        // 5. 检查审批权限
        ApprovalWorkflowEngine.ApprovalStep currentStep = approvalEngine.getCurrentStep(workflow, billData);
        if (!approvalEngine.canUserAudit(currentStep, userId, userRoles)) {
            return R.fail("无审批权限");
        }
        
        // 6. 执行审批操作(更新单据状态)
        String newStatus = executeApprovalAction(moduleCode, billId, action);
        
        // 7. 记录审批历史
        ErpApprovalHistory history = new ErpApprovalHistory();
        history.setBillId(Long.parseLong(billId.toString()));
        history.setModuleCode(moduleCode);
        history.setFlowId(flow.getFlowId());
        history.setCurrentStep(step);
        history.setApproverId(userId);
        history.setApprovalAction(action);
        history.setApprovalOpinion(opinion);
        approvalHistoryMapper.insert(history);
        
        // 8. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("billId", billId);
        result.put("action", action);
        result.put("status", newStatus);
        result.put("approver", userId);
        result.put("approvalTime", new Date());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("审批失败", e);
        return R.fail("审批失败：" + e.getMessage());
    }
}

// 辅助方法: 获取单据数据
private Map<String, Object> getBillData(String moduleCode, Object billId) {
    // 根据moduleCode调用对应的Service
    // 可以使用策略模式或工厂模式
    switch (moduleCode) {
        case "saleOrder":
            // SaleOrderService saleOrderService = ...
            // return saleOrderService.selectById(billId);
            break;
        case "deliveryOrder":
            // ...
            break;
        default:
            throw new ServiceException("不支持的模块: " + moduleCode);
    }
    return new HashMap<>();
}

// 辅助方法: 执行审批动作
private String executeApprovalAction(String moduleCode, Object billId, String action) {
    // 根据action更新单据状态
    String newStatus;
    switch (action) {
        case "AUDIT":
            newStatus = "C"; // 已审核
            break;
        case "REJECT":
            newStatus = "D"; // 已拒绝
            break;
        default:
            throw new ServiceException("不支持的审批动作: " + action);
    }
    
    // 更新单据状态(需要根据moduleCode调用对应Service)
    // ...
    
    return newStatus;
}
```

**TODO 3: 检查审批权限 (Line 224)**

```java
@PostMapping("/approval/check-permission")
public R<?> checkApprovalPermission(@RequestBody Map<String, Object> params) {
    try {
        Object billId = params.get("billId");
        String moduleCode = (String) params.get("moduleCode");
        String userId = (String) params.get("userId");
        List<String> userRoles = (List<String>) params.get("userRoles");
        
        // 1. 获取审批流程配置
        ErpApprovalFlowVo flow = approvalFlowService.getApprovalFlow(moduleCode);
        if (flow == null) {
            Map<String, Object> result = new HashMap<>();
            result.put("hasPermission", false);
            result.put("reason", "未找到审批流程配置");
            return R.ok(result);
        }
        
        // 2. 解析流程定义
        Map<String, Object> flowDefinition = JsonUtils.parseMap(flow.getFlowDefinition());
        List<Map<String, Object>> workflow = (List<Map<String, Object>>) flowDefinition.get("workflow");
        
        // 3. 获取单据数据
        Map<String, Object> billData = getBillData(moduleCode, billId);
        
        // 4. 获取当前审批步骤
        ApprovalWorkflowEngine.ApprovalStep currentStep = approvalEngine.getCurrentStep(workflow, billData);
        
        // 5. 检查权限
        boolean hasPermission = approvalEngine.canUserAudit(currentStep, userId, userRoles);
        
        // 6. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("hasPermission", hasPermission);
        result.put("currentStep", currentStep);
        if (!hasPermission) {
            result.put("reason", "用户角色不满足审批要求");
        }
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("权限检查失败", e);
        return R.fail("权限检查失败：" + e.getMessage());
    }
}
```

**TODO 4: 获取审批历史 (Line 244)**

```java
@GetMapping("/approval/history")
public R<?> getApprovalHistory(@RequestParam Map<String, String> params) {
    try {
        String billId = params.get("billId");
        String moduleCode = params.get("moduleCode");
        
        // 从数据库查询审批历史
        List<ErpApprovalHistory> historyList = approvalHistoryMapper.selectList(
            new LambdaQueryWrapper<ErpApprovalHistory>()
                .eq(ErpApprovalHistory::getBillId, Long.parseLong(billId))
                .eq(ErpApprovalHistory::getModuleCode, moduleCode)
                .orderByDesc(ErpApprovalHistory::getApprovalTime)
        );
        
        // 转换为VO
        List<Map<String, Object>> result = historyList.stream()
            .map(history -> {
                Map<String, Object> item = new HashMap<>();
                item.put("historyId", history.getHistoryId());
                item.put("step", history.getCurrentStep());
                item.put("approverId", history.getApproverId());
                item.put("action", history.getApprovalAction());
                item.put("opinion", history.getApprovalOpinion());
                item.put("approvalTime", history.getApprovalTime());
                return item;
            })
            .collect(Collectors.toList());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("获取审批历史失败", e);
        return R.fail("获取审批历史失败：" + e.getMessage());
    }
}
```

**TODO 5: 获取审批流程定义 (Line 258)**

```java
@GetMapping("/approval/workflow")
public R<?> getWorkflowDefinition(@RequestParam String moduleCode) {
    try {
        // 从数据库获取审批流程配置
        ErpApprovalFlowVo flow = approvalFlowService.getApprovalFlow(moduleCode);
        
        if (flow == null) {
            return R.fail("未找到审批流程配置");
        }
        
        // 解析流程定义
        Map<String, Object> flowDefinition = JsonUtils.parseMap(flow.getFlowDefinition());
        
        // 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("flowId", flow.getFlowId());
        result.put("flowName", flow.getFlowName());
        result.put("moduleCode", flow.getModuleCode());
        result.put("definition", flowDefinition);
        result.put("version", flow.getCurrentVersion());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("获取流程定义失败", e);
        return R.fail("获取流程定义失败：" + e.getMessage());
    }
}
```

**TODO 6: 转审操作 (Line 278)**

```java
@SaCheckPermission("k3:saleOrder:audit")
@PostMapping("/approval/transfer")
public R<?> transferApproval(@RequestBody Map<String, Object> params) {
    try {
        Object billId = params.get("billId");
        String moduleCode = (String) params.get("moduleCode");
        String targetUserId = (String) params.get("targetUserId");
        String reason = (String) params.get("reason");
        
        // 1. 获取当前用户
        LoginUser loginUser = LoginHelper.getLoginUser();
        String currentUserId = loginUser.getUserId();
        
        // 2. 获取审批流程配置
        ErpApprovalFlowVo flow = approvalFlowService.getApprovalFlow(moduleCode);
        
        // 3. 记录转审历史
        ErpApprovalHistory history = new ErpApprovalHistory();
        history.setBillId(Long.parseLong(billId.toString()));
        history.setModuleCode(moduleCode);
        history.setFlowId(flow.getFlowId());
        history.setApproverId(currentUserId);
        history.setApprovalAction("TRANSFER");
        history.setApprovalOpinion("转审给: " + targetUserId + ", 原因: " + reason);
        approvalHistoryMapper.insert(history);
        
        // 4. 更新单据的审批人(需要根据moduleCode调用对应Service)
        // ...
        
        // 5. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("billId", billId);
        result.put("transferredFrom", currentUserId);
        result.put("transferredTo", targetUserId);
        result.put("status", "transferred");
        result.put("transferTime", new Date());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("转审失败", e);
        return R.fail("转审失败：" + e.getMessage());
    }
}
```

**TODO 7: 撤回审批 (Line 301)**

```java
@SaCheckPermission("k3:saleOrder:audit")
@PostMapping("/approval/withdraw")
public R<?> withdrawApproval(@RequestBody Map<String, Object> params) {
    try {
        Object billId = params.get("billId");
        String moduleCode = (String) params.get("moduleCode");
        
        // 1. 获取当前用户
        LoginUser loginUser = LoginHelper.getLoginUser();
        String userId = loginUser.getUserId();
        
        // 2. 检查是否可以撤回(只有创建人或当前审批人可以撤回)
        // ...
        
        // 3. 获取审批流程配置
        ErpApprovalFlowVo flow = approvalFlowService.getApprovalFlow(moduleCode);
        
        // 4. 记录撤回历史
        ErpApprovalHistory history = new ErpApprovalHistory();
        history.setBillId(Long.parseLong(billId.toString()));
        history.setModuleCode(moduleCode);
        history.setFlowId(flow.getFlowId());
        history.setApproverId(userId);
        history.setApprovalAction("WITHDRAW");
        history.setApprovalOpinion("撤回审批");
        approvalHistoryMapper.insert(history);
        
        // 5. 更新单据状态为暂存
        // ...
        
        // 6. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("billId", billId);
        result.put("status", "withdrawn");
        result.put("withdrawTime", new Date());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("撤回审批失败", e);
        return R.fail("撤回审批失败：" + e.getMessage());
    }
}
```

#### 1.1.3 下推引擎TODO实现

**TODO 8: 获取可下推的目标列表 (Line 321)**

```java
@GetMapping("/push/targets")
public R<?> getPushTargets(@RequestParam String moduleCode) {
    try {
        // 从数据库查询可下推的目标模块
        List<ErpPushRelationVo> relations = pushRelationService.selectList(
            new ErpPushRelationBo()
                .setSourceModule(moduleCode)
                .setStatus("1")
        );
        
        // 转换为前端需要的格式
        List<Map<String, Object>> targets = relations.stream()
            .map(relation -> {
                Map<String, Object> target = new HashMap<>();
                target.put("relationId", relation.getRelationId());
                target.put("targetModule", relation.getTargetModule());
                target.put("relationName", relation.getRelationName());
                target.put("concurrencyControl", relation.getConcurrencyControl());
                return target;
            })
            .collect(Collectors.toList());
        
        return R.ok(targets);
    } catch (Exception e) {
        log.error("获取下推目标失败", e);
        return R.fail("获取下推目标失败：" + e.getMessage());
    }
}
```

**TODO 9: 执行下推操作 (Line 341)**

```java
@SaCheckPermission("k3:saleOrder:push")
@PostMapping("/push/execute")
public R<?> executePushDown(@RequestBody Map<String, Object> params) {
    try {
        Object sourceId = params.get("sourceId");
        String sourceModule = (String) params.get("sourceModule");
        String targetModule = (String) params.get("targetModule");
        Map<String, Object> confirmData = (Map<String, Object>) params.get("confirmData");
        
        // 1. 获取源单数据
        Map<String, Object> sourceData = getBillData(sourceModule, sourceId);
        if (sourceData == null) {
            return R.fail("源单不存在");
        }
        
        // 2. 获取映射配置
        ErpPushRelationVo relation = pushRelationService.selectOne(
            new ErpPushRelationBo()
                .setSourceModule(sourceModule)
                .setTargetModule(targetModule)
        );
        
        if (relation == null) {
            return R.fail("未找到下推关系配置");
        }
        
        // 3. 构建映射配置
        Map<String, Object> mappingConfig = new HashMap<>();
        mappingConfig.put("sourceToTarget", JsonUtils.parseMap(relation.getMappingRules()));
        mappingConfig.put("transformation", JsonUtils.parseMap(relation.getTransformationRules()));
        mappingConfig.put("validationRules", JsonUtils.parseMap(relation.getValidationRules()));
        
        // 4. 执行下推
        PushDownEngine.PushResult pushResult = pushDownEngine.execute(sourceData, mappingConfig);
        
        if (!pushResult.isSuccess()) {
            return R.fail(pushResult.getMessage());
        }
        
        // 5. 合并确认数据
        Map<String, Object> targetData = pushResult.getData();
        if (confirmData != null) {
            targetData.putAll(confirmData);
        }
        
        // 6. 保存目标单(需要根据targetModule调用对应Service)
        Object targetId = saveTargetData(targetModule, targetData);
        
        // 7. 返回结果
        Map<String, Object> result = new HashMap<>();
        result.put("sourceId", sourceId);
        result.put("targetId", targetId);
        result.put("targetModule", targetModule);
        result.put("status", "pushed");
        result.put("pushTime", new Date());
        
        return R.ok(result);
    } catch (Exception e) {
        log.error("下推失败", e);
        return R.fail("下推失败：" + e.getMessage());
    }
}

// 辅助方法: 保存目标单数据
private Object saveTargetData(String targetModule, Map<String, Object> targetData) {
    // 根据targetModule调用对应的Service
    switch (targetModule) {
        case "deliveryOrder":
            // DeliveryOrderService deliveryOrderService = ...
            // return deliveryOrderService.insert(targetData);
            break;
        case "purchaseOrder":
            // ...
            break;
        default:
            throw new ServiceException("不支持的目标模块: " + targetModule);
    }
    return null;
}
```

**TODO 10-16**: 类似实现,详见完整代码

---

### 1.2 ScriptEngine安全加固

#### 1.2.1 添加Aviator依赖

```xml
<!-- pom.xml -->
<dependency>
    <groupId>com.googlecode.aviator</groupId>
    <artifactId>aviator</artifactId>
    <version>5.3.3</version>
</dependency>
```

#### 1.2.2 修改ApprovalWorkflowEngine

```java
package com.ruoyi.system.service.engine;

import com.googlecode.aviator.AviatorEvaluator;
import com.googlecode.aviator.Expression;
import com.ruoyi.common.core.utils.StringUtils;
import lombok.Data;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.util.List;
import java.util.Map;

/**
 * 审批流程引擎(安全加固版)
 * 使用Aviator表达式引擎替代ScriptEngine
 * 
 * @author ERP Development Team
 * @date 2026-03-22
 */
@Slf4j
@Component
public class ApprovalWorkflowEngine {

    /**
     * 获取当前审批步骤
     */
    @SuppressWarnings("unchecked")
    public ApprovalStep getCurrentStep(List<Map<String, Object>> workflow, Map<String, Object> billData) {
        if (workflow == null || workflow.isEmpty() || billData == null) {
            return null;
        }

        try {
            for (Map<String, Object> step : workflow) {
                String condition = (String) step.get("condition");
                
                if (StringUtils.isEmpty(condition)) {
                    return convertToApprovalStep(step);
                }

                // 使用Aviator评估条件
                if (evaluateCondition(condition, billData)) {
                    return convertToApprovalStep(step);
                }
            }

        } catch (Exception e) {
            log.error("获取审批步骤失败", e);
        }

        return null;
    }

    /**
     * 评估条件表达式(使用Aviator,安全)
     */
    public boolean evaluateCondition(String condition, Map<String, Object> context) {
        if (StringUtils.isEmpty(condition)) {
            return false;
        }

        try {
            // 编译表达式(可缓存)
            Expression expression = AviatorEvaluator.compile(condition, true);
            
            // 执行表达式
            Object result = expression.execute(context);
            
            if (result instanceof Boolean) {
                return (Boolean) result;
            }
            
            return Boolean.parseBoolean(result.toString());

        } catch (Exception e) {
            log.error("条件表达式执行失败：{}", condition, e);
            return false;
        }
    }

    /**
     * 检查用户是否可以审批
     */
    @SuppressWarnings("unchecked")
    public boolean canUserAudit(ApprovalStep step, String userId, List<String> userRoles) {
        if (step == null || userRoles == null || userRoles.isEmpty()) {
            return false;
        }

        try {
            Object roleObj = step.getRole();
            if (roleObj == null) {
                return true;
            }

            if (roleObj instanceof String) {
                return userRoles.contains(roleObj.toString());
            }

            if (roleObj instanceof List) {
                List<String> requiredRoles = (List<String>) roleObj;
                for (String role : requiredRoles) {
                    if (userRoles.contains(role)) {
                        return true;
                    }
                }
                return false;
            }

        } catch (Exception e) {
            log.error("检查用户审批权限失败", e);
            return false;
        }

        return false;
    }

    /**
     * 转换为 ApprovalStep 对象
     */
    private ApprovalStep convertToApprovalStep(Map<String, Object> stepMap) {
        ApprovalStep step = new ApprovalStep();
        
        if (stepMap.get("step") instanceof Number) {
            step.setStep(((Number) stepMap.get("step")).intValue());
        }
        step.setName((String) stepMap.get("name"));
        step.setRole((String) stepMap.get("role"));
        step.setAction((String) stepMap.get("action"));
        step.setCondition((String) stepMap.get("condition"));
        
        if (stepMap.get("required") instanceof Boolean) {
            step.setRequired((Boolean) stepMap.get("required"));
        } else {
            step.setRequired(true);
        }

        return step;
    }

    /**
     * 审批步骤信息
     */
    @Data
    public static class ApprovalStep {
        private Integer step;
        private String name;
        private String role;
        private String action;
        private String condition;
        private Boolean required;
    }
}
```

#### 1.2.3 修改PushDownEngine

类似地,将PushDownEngine中的ScriptEngine替换为Aviator。

---

### 1.3 添加SQL字段白名单校验

#### 1.3.1 修改DynamicQueryEngine

```java
package com.ruoyi.system.service.engine;

import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;
import com.ruoyi.common.core.utils.StringUtils;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Component;

import java.util.Map;
import java.util.Set;

/**
 * 动态查询引擎(安全加固版)
 * 添加字段白名单校验,防止SQL注入
 * 
 * @author ERP Development Team
 * @date 2026-03-22
 */
@Slf4j
@Component
public class DynamicQueryEngine {

    /**
     * 允许查询的字段白名单
     * 根据实际业务配置
     */
    private static final Set<String> ALLOWED_FIELDS = Set.of(
        // 销售订单字段
        "fbillNo", "fOraBaseProperty", "fDocumentStatus", "fBillAmount",
        "fdate", "fCustomerNumber", "fCustomerName", "fCreatorId",
        "fCreateDate", "fModifierId", "fModifyDate",
        
        // 发货通知单字段
        "sourceBillNo", "bizDate", "customerNumber", "customerName",
        
        // 通用字段
        "id", "createTime", "updateTime", "status", "version"
    );

    /**
     * 根据配置构建查询条件
     */
    @SuppressWarnings("unchecked")
    public <T> QueryWrapper<T> buildQueryConditions(
            QueryWrapper<T> queryWrapper,
            Map<String, Object> searchConfig,
            Map<String, Object> queryParams) {
        
        if (searchConfig == null || queryParams == null) {
            return queryWrapper;
        }

        try {
            Object fieldsObj = searchConfig.get("fields");
            if (!(fieldsObj instanceof java.util.List)) {
                return queryWrapper;
            }

            java.util.List<Map<String, Object>> fields = 
                (java.util.List<Map<String, Object>>) fieldsObj;

            for (Map<String, Object> fieldConfig : fields) {
                String field = (String) fieldConfig.get("field");
                String searchType = (String) fieldConfig.get("searchType");
                
                if (StringUtils.isEmpty(field)) {
                    continue;
                }

                // ✅ 字段白名单校验
                if (!isValidField(field)) {
                    log.warn("非法字段访问尝试: {}", field);
                    continue;
                }

                Object value = queryParams.get(field);
                if (value == null || StringUtils.isEmpty(value.toString())) {
                    continue;
                }

                // 根据搜索类型构建查询条件
                switch (StringUtils.defaultString(searchType)) {
                    case "like":
                        queryWrapper.like(field, value.toString());
                        break;
                    
                    case "left_like":
                        queryWrapper.likeLeft(field, value.toString());
                        break;
                    
                    case "right_like":
                        queryWrapper.likeRight(field, value.toString());
                        break;
                    
                    case "in":
                        if (value instanceof java.util.Collection) {
                            queryWrapper.in(field, (java.util.Collection<?>) value);
                        } else {
                            queryWrapper.eq(field, value);
                        }
                        break;
                    
                    case "between":
                        handleBetweenCondition(queryWrapper, field, value);
                        break;
                    
                    case "gt":
                        queryWrapper.gt(field, value);
                        break;
                    
                    case "ge":
                        queryWrapper.ge(field, value);
                        break;
                    
                    case "lt":
                        queryWrapper.lt(field, value);
                        break;
                    
                    case "le":
                        queryWrapper.le(field, value);
                        break;
                    
                    case "ne":
                        queryWrapper.ne(field, value);
                        break;
                    
                    default:
                        queryWrapper.eq(field, value);
                }
            }

            log.info("动态查询条件构建成功，字段数：{}", fields.size());

        } catch (Exception e) {
            log.error("动态查询条件构建失败", e);
        }

        return queryWrapper;
    }

    /**
     * 校验字段是否合法
     */
    private boolean isValidField(String field) {
        // 方案1: 白名单校验
        if (ALLOWED_FIELDS.contains(field)) {
            return true;
        }
        
        // 方案2: 正则表达式校验(只允许字母、数字、下划线)
        // return field != null && field.matches("^[a-zA-Z_][a-zA-Z0-9_]*$");
        
        return false;
    }

    /**
     * 处理 Between 查询条件(改进版)
     */
    @SuppressWarnings("unchecked")
    private <T> void handleBetweenCondition(
            QueryWrapper<T> queryWrapper,
            String field, 
            Object value) {
        
        if (value instanceof Map) {
            Map<String, Object> range = (Map<String, Object>) value;
            Object start = range.get("start");
            Object end = range.get("end");
            
            // 类型校验
            if (start != null && end != null) {
                try {
                    if (start instanceof Comparable && end instanceof Comparable) {
                        queryWrapper.between(field, start, end);
                    }
                } catch (Exception e) {
                    log.error("Between条件构建失败: field={}, start={}, end={}", 
                        field, start, end, e);
                }
            }
        } else if (value instanceof String) {
            String[] parts = value.toString().split(",");
            if (parts.length == 2) {
                queryWrapper.between(field, parts[0], parts[1]);
            }
        }
    }

    /**
     * 解析排序配置
     */
    public <T> void applySortConfig(
            QueryWrapper<T> queryWrapper,
            Map<String, Object> sortConfig) {
        
        if (sortConfig == null) {
            return;
        }

        String orderBy = (String) sortConfig.get("orderBy");
        String orderDirection = (String) sortConfig.get("orderDirection");

        if (StringUtils.isNotEmpty(orderBy)) {
            // 排序字段也需要校验
            if (!isValidField(orderBy)) {
                log.warn("非法排序字段: {}", orderBy);
                return;
            }
            
            if ("asc".equalsIgnoreCase(orderDirection)) {
                queryWrapper.orderByAsc(true, camelToUnderline(orderBy));
            } else {
                queryWrapper.orderByDesc(true, camelToUnderline(orderBy));
            }
        }
    }

    /**
     * 驼峰转下划线
     */
    private String camelToUnderline(String str) {
        if (str == null || str.isEmpty()) {
            return str;
        }
        StringBuilder sb = new StringBuilder();
        sb.append(Character.toLowerCase(str.charAt(0)));
        for (int i = 1; i < str.length(); i++) {
            char c = str.charAt(i);
            if (Character.isUpperCase(c)) {
                sb.append('_');
                sb.append(Character.toLowerCase(c));
            } else {
                sb.append(c);
            }
        }
        return sb.toString();
    }
}
```

---

## 二、P1级别修复方案

### 2.1 补全接口权限注解

```java
// ErpEngineController.java

// 添加权限注解
@SaCheckPermission("k3:saleOrder:query")
@PostMapping("/validation/execute")
public R<?> executeValidation(...) { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/query/types")
public R<?> getAvailableQueryTypes() { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/validation/rules")
public R<?> getAvailableValidationRules() { }

@SaCheckPermission("k3:saleOrder:query")
@PostMapping("/validation/field")
public R<?> validateField(...) { }

@SaCheckPermission("k3:saleOrder:query")
@PostMapping("/approval/current-step")
public R<?> getCurrentApprovalStep(...) { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/approval/history")
public R<?> getApprovalHistory(...) { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/approval/workflow")
public R<?> getWorkflowDefinition(...) { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/push/targets")
public R<?> getPushTargets(...) { }

@SaCheckPermission("k3:saleOrder:query")
@PostMapping("/push/preview")
public R<?> previewPushDown(...) { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/push/mapping")
public R<?> getPushMappingConfig(...) { }

@SaCheckPermission("k3:saleOrder:query")
@PostMapping("/push/validate")
public R<?> validatePushData(...) { }

@SaCheckPermission("k3:saleOrder:query")
@GetMapping("/push/history")
public R<?> getPushHistory(...) { }
```

### 2.2 添加数据库外键约束和索引

创建SQL脚本: `ERP 配置化方案数据库优化脚本.sql`

```sql
-- ============================================
-- ERP 配置化方案数据库优化脚本
-- 执行时间: 2026-03-22
-- 说明: 添加外键约束和索引优化
-- ============================================

USE test;

-- ============================================
-- 1. 添加外键约束
-- ============================================

-- 1.1 erp_page_config_history 外键
ALTER TABLE erp_page_config_history 
ADD CONSTRAINT fk_history_config 
FOREIGN KEY (config_id) REFERENCES erp_page_config(config_id)
ON DELETE CASCADE;

-- 1.2 erp_approval_history 外键
ALTER TABLE erp_approval_history 
ADD CONSTRAINT fk_history_flow 
FOREIGN KEY (flow_id) REFERENCES erp_approval_flow(flow_id)
ON DELETE SET NULL;

-- 1.3 erp_page_config 父配置外键
ALTER TABLE erp_page_config
ADD CONSTRAINT fk_parent_config 
FOREIGN KEY (parent_config_id) REFERENCES erp_page_config(config_id)
ON DELETE SET NULL;

-- ============================================
-- 2. 添加复合索引
-- ============================================

-- 2.1 erp_approval_history 复合索引
ALTER TABLE erp_approval_history 
ADD INDEX idx_module_bill (module_code, bill_id);

ALTER TABLE erp_approval_history
ADD INDEX idx_bill_action (bill_id, approval_action);

-- 2.2 erp_page_config 复合索引
ALTER TABLE erp_page_config
ADD INDEX idx_module_status_version (module_code, status, version);

-- 2.3 erp_push_relation 复合索引
ALTER TABLE erp_push_relation
ADD INDEX idx_source_status (source_module, status);

-- ============================================
-- 3. 添加全文索引(JSON字段搜索)
-- ============================================

ALTER TABLE erp_page_config 
ADD FULLTEXT INDEX ft_config_content (config_content);

ALTER TABLE erp_approval_flow
ADD FULLTEXT INDEX ft_flow_definition (flow_definition);

-- ============================================
-- 4. 添加CHECK约束(MySQL 8.0+)
-- ============================================

-- 如果使用MySQL 8.0+,可以添加CHECK约束
-- ALTER TABLE erp_page_config
-- ADD CONSTRAINT chk_config_content 
-- CHECK (LENGTH(config_content) <= 1048576);

-- ALTER TABLE erp_push_relation
-- ADD CONSTRAINT chk_concurrency 
-- CHECK (concurrency_control IN ('optimistic', 'pessimistic'));

-- ============================================
-- 5. 补充触发器
-- ============================================

DELIMITER $$

-- 5.1 添加INSERT触发器
CREATE TRIGGER `trg_erp_config_history_insert`
AFTER INSERT ON `erp_page_config`
FOR EACH ROW
BEGIN
  INSERT INTO erp_page_config_history (
    config_id, module_code, config_type, version,
    config_content, change_reason, change_type, create_by
  ) VALUES (
    NEW.config_id, NEW.module_code, NEW.config_type, NEW.version,
    NEW.config_content, '初始创建', 'ADD', NEW.create_by
  );
END$$

-- 5.2 添加DELETE触发器
CREATE TRIGGER `trg_erp_config_history_delete`
BEFORE DELETE ON `erp_page_config`
FOR EACH ROW
BEGIN
  INSERT INTO erp_page_config_history (
    config_id, module_code, config_type, version,
    config_content, change_reason, change_type, create_by
  ) VALUES (
    OLD.config_id, OLD.module_code, OLD.config_type, OLD.version,
    OLD.config_content, '配置删除', 'DELETE', OLD.update_by
  );
END$$

DELIMITER ;

-- ============================================
-- 6. 验证优化结果
-- ============================================

-- 查看表结构
SHOW CREATE TABLE erp_page_config;
SHOW CREATE TABLE erp_page_config_history;
SHOW CREATE TABLE erp_approval_history;

-- 查看索引
SHOW INDEX FROM erp_page_config;
SHOW INDEX FROM erp_approval_history;

-- ============================================
-- 执行完成
-- ============================================
```

### 2.3 优化缓存键设计

```java
// ErpPageConfigServiceImpl.java

@Override
public String getPageConfig(String moduleCode, String configType) {
    // 缓存键包含configType,避免冲突
    String cacheKey = moduleCode + ":" + configType;
    
    // 先从缓存获取
    Object cached = CacheUtils.get(CacheNames.ERP_CONFIG, cacheKey);
    if (ObjectUtil.isNotNull(cached)) {
        return cached.toString();
    }
    
    // 缓存未命中,从数据库查询
    ErpPageConfig config = pageConfigMapper.selectOne(new LambdaQueryWrapper<ErpPageConfig>()
        .eq(ErpPageConfig::getModuleCode, moduleCode)
        .eq(ErpPageConfig::getConfigType, configType)
        .eq(ErpPageConfig::getStatus, "1")
        .orderByDesc(ErpPageConfig::getVersion)
        .last("LIMIT 1"));
    
    if (ObjectUtil.isNull(config)) {
        return null;
    }
    
    String content = config.getConfigContent();
    
    // 放入缓存 (TTL: 1小时)
    CacheUtils.put(CacheNames.ERP_CONFIG, cacheKey, content, 3600);
    
    return content;
}

// 更新时也要清除对应的缓存
@Override
@Transactional(rollbackFor = Exception.class)
public int updateByBo(ErpPageConfigBo bo) {
    ErpPageConfig config = MapstructUtils.convert(bo, ErpPageConfig.class);
    
    Integer newVersion = bo.getVersion() + 1;
    config.setVersion(newVersion);
    
    int row = pageConfigMapper.updateById(config);
    
    if (row > 0) {
        // 清除缓存(使用正确的缓存键)
        String cacheKey = config.getModuleCode() + ":" + config.getConfigType();
        CacheUtils.evict(CacheNames.ERP_CONFIG, cacheKey);
    }
    
    return row;
}
```

---

## 三、修复验证清单

### 3.1 功能验证

- [ ] 审批流程功能可用
  - [ ] 获取当前审批步骤
  - [ ] 执行审批操作
  - [ ] 检查审批权限
  - [ ] 获取审批历史
  - [ ] 转审操作
  - [ ] 撤回审批

- [ ] 下推引擎功能可用
  - [ ] 获取下推目标
  - [ ] 执行下推操作
  - [ ] 预览下推数据
  - [ ] 批量下推
  - [ ] 取消下推

- [ ] 动态查询功能可用
  - [ ] 根据moduleCode执行查询

### 3.2 安全验证

- [ ] ScriptEngine已替换为Aviator
- [ ] SQL字段白名单校验生效
- [ ] 所有接口都有权限注解

### 3.3 性能验证

- [ ] 数据库外键约束已添加
- [ ] 索引已优化
- [ ] 缓存键设计已优化

---

## 四、修复完成标准

### 4.1 代码标准

- ✅ 所有TODO已实现
- ✅ 无编译错误
- ✅ 无安全风险
- ✅ 代码规范

### 4.2 功能标准

- ✅ 审批流程功能完整可用
- ✅ 下推引擎功能完整可用
- ✅ 动态查询功能完整可用

### 4.3 测试标准

- ✅ 单元测试通过
- ✅ 集成测试通过
- ✅ 安全测试通过

---

**修复方案完成时间**: 2026-03-22  
**预计实施时间**: 5-7人天  
**下次更新**: 实施完成后进行验证测试
