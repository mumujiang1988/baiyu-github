# 强制终止所有java进程（避免端口占用、进程冲突） 
taskkill /f /im java.exe
taskkill /f /im javaw.exe

# 切换到项目根目录（请确认路径正确性，若路径不同需手动修改）
cd D:\baiyuyunma\baiyu-github\baiyu-github\baiyu-ruoyi

# 批量删除指定模块的target目录（整合命令，减少重复输入）
$targetPaths = @(
    ".\ruoyi-modules\ruoyi-system\target",
    ".\ruoyi-modules\ruoyi-demo\target",
    ".\ruoyi-modules\ruoyi-erp-api\target",
    ".\ruoyi-modules\ruoyi-generator\target",
    ".\ruoyi-admin-wms\target"
)
Remove-Item -Recurse -Force $targetPaths -ErrorAction SilentlyContinue

# 多线程编译（-T 4 表示4线程，可根据电脑CPU核心数调整，如8核可设为-T 8）
mvn clean install -DskipTests -T 16

# 启动ruoyi-admin-wms服务
cd D:\baiyuyunma\baiyu-github\baiyu-github\baiyu-ruoyi\ruoyi-admin-wms
mvn spring-boot:run

# 启动前端项目（新开PowerShell窗口执行，避免阻塞后端服务）
Start-Process powershell -ArgumentList "cd D:\baiyuyunma\baiyu-github\baiyu-github\baiyu-web; npm run dev"