# erp-lowcode-dev-assistant 技能更新报告 - v5.0.1

**更新时间**: 2026-03-25  
**更新版本**: v5.0.1  
**更新内容**: 新增"引用不存在的自定义 API 路径"错误检查

---

## 🎯 更新概述

本次更新将刚才修复的 API 路径错误案例纳入技能知识库，帮助未来的开发避免类似的错误。

---

## ✅ 更新内容

### 1. 技能概述增强

**更新前**:
```markdown
本技能专为 RuoYi-WMS 低代码 ERP 模块开发设计，提供：
1. **前后端错误审计** - 自动检测和修复前后端 API 交互问题
2. **页面配置编写** - 生成符合规范的 JSON 配置文件
3. **字段一致性检查** - 确保数据库、前端、配置三者字段名一致
4. **API 路径验证** - 验证通用引擎 API 的正确性
5. **⭐ 多表格查询** - 单页面多表格独立查询配置（v4.1.0+）
```

**更新后**:
```markdown
本技能专为 RuoYi-WMS 低代码 ERP 模块开发设计，提供：
1. **前后端错误审计** - 自动检测和修复前后端 API 交互问题
2. **页面配置编写** - 生成符合规范的 JSON 配置文件
3. **字段一致性检查** - 确保数据库、前端、配置三者字段名一致
4. **API 路径验证** - 验证通用引擎 API 的正确性
5. **⭐ 多表格查询** - 单页面多表格独立查询配置（v4.1.0+）
6. **⭐ 构建器模式复用** - 主表/明细表/字典查询复用（v5.0.0+）
```

**新增**: 第 6 点 - 构建器模式复用能力说明

---

### 2. 后端接口检查清单更新

**更新前**:
```markdown
1.  后端接口检查
   - /erp/config/get/{moduleCode} - 配置获取接口
   - /erp/engine/list - 列表查询接口
   - /erp/engine/custom/entry - 明细查询接口
   - /erp/engine/custom/cost - 成本查询接口
```

**更新后**:
```markdown
1.  后端接口检查
   - /erp/config/get/{moduleCode} - 配置获取接口
   - /erp/engine/query/execute - 通用查询接口（⭐ 强制使用）
   - /erp/engine/dictionary/{name}/data - 字典查询接口（⭐ 复用方案）
   - ❌ /erp/engine/custom/entry - 不存在，禁止使用
   - ❌ /erp/engine/custom/cost - 不存在，禁止使用
```

**改进**:
- ✅ 明确标注 `/custom/*` 接口不存在
- ✅ 推荐使用通用查询接口
- ✅ 添加字典查询接口说明

---

### 3. 前端配置检查增强

**更新前**:
```markdown
2.  前端配置检查
   - ERPConfigParser.loadFromDatabase() 方法是否存在
   - request import 是否正确
   - moduleCode 是否传递正确
```

**更新后**:
```markdown
2.  前端配置检查
   - ERPConfigParser.loadFromDatabase() 方法是否存在
   - request import 是否正确
   - moduleCode 是否传递正确
   - ⭐ 是否使用了不存在的自定义 API 路径
```

**新增**: 检查是否使用了不存在的 API 路径

---

### 4. 数据库配置检查增强

**更新前**:
```markdown
3.  数据库配置检查
   - erp_page_config 表是否存在记录
   - config_content JSON 格式是否正确
   - moduleCode 大小写是否一致
```

**更新后**:
```markdown
3.  数据库配置检查
   - erp_page_config 表是否存在记录
   - config_content JSON 格式是否正确
   - moduleCode 大小写是否一致
   - ⭐ 是否引用了不存在的后端接口
```

**新增**: 检查配置是否引用了不存在的后端接口

---

### 5. 新增错误案例：错误 4

**全新内容** (v5.0.1 新增):

#### 错误 4：⭐ 引用不存在的自定义 API 路径

**错误现象**:
```json
// ❌ 错误配置：引用不存在的后端接口
{
  "expandRow": {
    "tabs": [
      {
        "name": "entry",
        "api": "/erp/engine/custom/entry?moduleCode={moduleCode}&billNo={billNo}"
      },
      {
        "name": "cost",
        "api": "/erp/engine/custom/cost?moduleCode={moduleCode}&billNo={billNo}"
      }
    ]
  }
}
```

**问题分析**:
- ❌ 后端不存在 `/erp/engine/custom/entry` 接口
- ❌ 后端不存在 `/erp/engine/custom/cost` 接口
- ❌ 违反了构建器模式的配置规范
- ❌ 会导致浏览器控制台出现 404 错误

**正确做法**:
```json
// ✅ 正确配置：使用 subTableQueryConfigs + multiTableQueryBuilder
{
  "subTableQueryConfigs": {
    "entry": {
      "enabled": true,
      "tableName": "t_sale_order_entry",
      "defaultConditions": [...],
      "defaultOrderBy": [...]
    },
    "cost": {
      "enabled": true,
      "tableName": "t_sale_order_cost",
      "defaultConditions": [...],
      "defaultOrderBy": [...]
    }
  },
  "expandRow": {
    "tabs": [
      {
        "name": "entry",
        "label": "销售订单明细",
        "dataField": "entryList",
        // ✅ 移除错误的 API，数据由 multiTableQueryBuilder 加载
        "table": {...}
      }
    ]
  }
}
```

**前端调用示例**:
```javascript
import multiTableQueryBuilder from '../utils/multiTableQueryBuilder'

const subTableConfigs = multiTableQueryBuilder.parseSubTableConfigs(config.value)
const results = await multiTableQueryBuilder.queryAllSubTables(
  moduleCode,
  subTableConfigs,
  { billNo }
)

if (results.entry) {
  entryList.value = results.entry.data
}
```

**后端实现**:
```java
@PostMapping("/query/execute")
public R<?> executeDynamicQuery(@RequestBody Map<String, Object> params) {
    String tableName = params.get("tableName");
    Map<String, Object> queryConfig = params.get("queryConfig");
    
    QueryWrapper<Object> queryWrapper = buildQueryFromBuilderMode(queryConfig);
    
    Page<Map<String, Object>> page = dataPermissionService
        .selectPageByModuleWithTableName(moduleCode, tableName, pageQuery, queryWrapper);
    
    return R.ok(Map.of("rows", page.getRecords(), "total", page.getTotal()));
}
```

**核心优势**:
- ✅ 统一的接口，易于维护
- ✅ 符合构建器模式规范
- ✅ 无需编写额外的 Controller
- ✅ 配置驱动，灵活可扩展
- ✅ 避免 404 错误

**检查清单**:
- [ ] JSON 配置中是否存在 `/custom/*` 路径？
- [ ] 是否使用了 `subTableQueryConfigs` 配置？
- [ ] 前端是否使用了 `multiTableQueryBuilder`？
- [ ] 后端接口是否实际存在？（搜索代码确认）

---

## 📊 更新统计

| 更新项 | 修改行数 | 新增内容 |
|--------|---------|---------|
| 技能概述 | +1 行 | 构建器模式复用说明 |
| 后端接口检查 | +5 行 | 标注不存在的接口 |
| 前端配置检查 | +1 行 | API 路径检查 |
| 数据库配置检查 | +1 行 | 接口存在性检查 |
| 错误案例 | +148 行 | 完整的错误 4 案例 |

**总计**: +156 行新增内容

---

## 🎯 核心价值

### 知识沉淀

**从实际问题到技能知识**:
1. ✅ 用户发现 API 路径命名问题
2. ✅ 深入分析发现后端接口不存在
3. ✅ 修复 JSON 配置中的错误引用
4. ✅ 将经验纳入技能知识库

---

### 预防机制

**技能现在可以主动检查**:
- ✅ JSON 配置中是否包含 `/custom/*` 路径
- ✅ 配置引用的后端接口是否实际存在
- ✅ 是否使用了正确的构建器模式
- ✅ 是否符合多表格查询规范

---

### 开发指导

**为开发者提供**:
- ✅ 明确的错误示例和正确做法
- ✅ 完整的前后端代码模板
- ✅ 清晰的检查清单
- ✅ 核心优势的详细说明

---

## 📚 相关文档

- [API 路径命名问题分析报告.md](./API 路径命名问题分析报告.md)
- [API 路径修复报告.md](./API 路径修复报告.md)
- [business.config.template.json 配置闭环审计报告.md](./business.config.template.json 配置闭环审计报告.md)
- [SKILL.md](./.lingma/skills/erp-lowcode-dev-assistant/SKILL.md)

---

## 🎉 总结

### ✅ 已完成的工作

1. ✅ 更新了技能概述，增加构建器模式复用说明
2. ✅ 更新了后端接口检查清单，标注不存在的接口
3. ✅ 更新了前端和数据库配置检查项
4. ✅ 新增了"错误 4"完整案例（148 行）
5. ✅ 提供了完整的检查清单和代码示例

---

### 📈 能力提升

**技能版本**: v5.0.0 → v5.0.1

**能力提升**:
- ✅ 错误检测能力增强
- ✅ 配置规范性提升
- ✅ 开发指导更全面
- ✅ 预防机制更完善

---

### 🚀 长期价值

**对团队的价值**:
- ✅ 避免重复踩坑
- ✅ 统一开发规范
- ✅ 提升代码质量
- ✅ 降低维护成本

**对项目的价值**:
- ✅ 减少前后端不一致
- ✅ 消除 404 错误
- ✅ 强化构建器模式应用
- ✅ 提升系统稳定性

---

**更新人员**: AI Assistant (erp-lowcode-dev-assistant)  
**更新日期**: 2026-03-25  
**技能版本**: v5.0.1  
**适用框架**: RuoYi-WMS + Vue 3 + Element Plus
